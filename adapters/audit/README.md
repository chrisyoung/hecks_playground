# audit/

Command-execution audit trail. Records who-did-what-when across the bus.

| File | What |
|---|---|
| `audit.rb` | Ruby extension that wraps the bus to write an audit entry per command. Pre-bluebook ; lift target. |
