# llm/

Model adapters — speak prompts, receive completions.

| Adapter | What |
|---|---|
| `ollama/` | **(not copied — source bluebook flags 9 validator errors)** ; original at `../../hecks_conception/adapters/ollama.bluebook`. |
| `claude/` | Ruby Claude HTTP + CLI adapters. Pre-bluebook ; lift target. |
