# storage/

Driven adapters that persist aggregate state and events.

| Adapter | What |
|---|---|
| `heki/` | **(not copied — source bluebook flags 11 validator errors)** ; original at `../../hecks_conception/adapters/heki.bluebook`. |
| `r2/` | Bluebook + behaviors for Cloudflare R2 object storage. |
| `filesystem_store/` | Plain-filesystem store. Pre-bluebook. |
| `mysql/` | MySQL store. Pre-bluebook. |
| `postgres/` | Postgres store. Pre-bluebook. |
| `sqlite/` | SQLite store. Pre-bluebook. |
