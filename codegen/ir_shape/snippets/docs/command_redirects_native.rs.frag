    /// The native harness tool this command is the governed door for —
    /// `redirects_native "Bash"` on `Tools::ShellTool.Bash`. Domain structure,
    /// read from the IR : the macrophage's governed-channel rule PROJECTS the
    /// native-tool -> door map from every command carrying this, and derives the
    /// door_args hint from the command's own attributes (no second source). A
    /// command without it is not a door ; its native tool (if any) is ungoverned.
