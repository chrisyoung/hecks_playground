# worker-build / cargo artifacts — regenerated by `worker-build --release`
/build/
/target/
# wrangler dev --local's miniflare state (R2/cache/workflows persistence)
/.wrangler/
# Cargo.lock IS tracked (mirrors pigeoncoop/worker, i766) -- a floating
# lockfile let wasm-bindgen drift to 0.2.127 and broke the wasm build
# with "externref table required for catch wrappers" (i768 slice 2.2).
# Pin it, commit it, bump deliberately.
