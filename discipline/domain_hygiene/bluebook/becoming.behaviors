Hecks.behaviors "Becoming" do
  vision "Behavioral tests for the Becoming domain — exercises every command and query in memory"

  # i111-C : phase aggregates collapsed into Harvest. Every test now
  # targets Harvest.

  # ── Phase 1: Harvest ──────────────────────────────────────────

  test "ScanNursery sets nursery_path" do
    tests "ScanNursery", on: "Harvest"
    input  nursery_path: "sample"
    expect nursery_ready: true
  end

  test "HarvestDomains sets nursery_path" do
    tests "HarvestDomains", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    input  nursery_path: "sample"
    expect ok: "true"
  end

  test "HarvestDomains cascades through policy chain" do
    tests "HarvestDomains", on: "Harvest", kind: :cascade
    setup  "VaryPrompts"
    setup  "AddCrossAggregateFlows"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "RunEvaluation"
    setup  "ScanNursery", nursery_path: "sample"
    input  nursery_path: "sample"
    expect emits: ["DomainsHarvested", "PromptsVaried", "NegativesGenerated", "ChainOfThoughtAdded", "CategoriesBalanced", "CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "HarvestConversations runs" do
    tests "HarvestConversations", on: "Harvest"
    expect ok: "true"
  end

  test "HarvestDreams runs" do
    tests "HarvestDreams", on: "Harvest"
    expect ok: "true"
  end

  test "HarvestIdentity runs" do
    tests "HarvestIdentity", on: "Harvest"
    expect ok: "true"
  end


  # ── Phase 2: Shape (was Shape aggregate) ──────────────────────────────────────────

  test "VaryPrompts runs" do
    tests "VaryPrompts", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "VaryPrompts cascades through policy chain" do
    tests "VaryPrompts", on: "Harvest", kind: :cascade
    setup  "ScanNursery", nursery_path: "sample"
    setup  "AddCrossAggregateFlows"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "RunEvaluation"
    expect emits: ["PromptsVaried", "NegativesGenerated", "ChainOfThoughtAdded", "CategoriesBalanced", "CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "GenerateNegatives runs" do
    tests "GenerateNegatives", on: "Harvest"
    expect ok: "true"
  end

  test "GenerateNegatives cascades through policy chain" do
    tests "GenerateNegatives", on: "Harvest", kind: :cascade
    setup  "ScanNursery", nursery_path: "sample"
    setup  "AddCrossAggregateFlows"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "RunEvaluation"
    expect emits: ["NegativesGenerated", "ChainOfThoughtAdded", "CategoriesBalanced", "CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "AddChainOfThought runs" do
    tests "AddChainOfThought", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "AddChainOfThought cascades through policy chain" do
    tests "AddChainOfThought", on: "Harvest", kind: :cascade
    setup  "ScanNursery", nursery_path: "sample"
    setup  "AddCrossAggregateFlows"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "RunEvaluation"
    expect emits: ["ChainOfThoughtAdded", "CategoriesBalanced", "CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "CountCategories runs" do
    tests "CountCategories", on: "Harvest"
    expect categories_counted: true
  end

  test "BalanceCategories runs" do
    tests "BalanceCategories", on: "Harvest"
    expect ok: "true"
  end

  test "BalanceCategories cascades through policy chain" do
    tests "BalanceCategories", on: "Harvest", kind: :cascade
    setup  "AddCrossAggregateFlows"
    setup  "ScanNursery", nursery_path: "sample"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "RunEvaluation"
    expect emits: ["CategoriesBalanced", "CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end


  # ── Phase 3: Enrich (was Enrich aggregate) ──────────────────────────────────────────

  test "AddCrossAggregateFlows runs" do
    tests "AddCrossAggregateFlows", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "AddCrossAggregateFlows cascades through policy chain" do
    tests "AddCrossAggregateFlows", on: "Harvest", kind: :cascade
    setup  "ScanNursery", nursery_path: "sample"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "VaryPrompts"
    setup  "RunEvaluation"
    expect emits: ["CrossAggregateFlowsAdded", "WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "AddSpecifications runs" do
    tests "AddSpecifications", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "AddCollections runs" do
    tests "AddCollections", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "DeepenWorkflows runs" do
    tests "DeepenWorkflows", on: "Harvest"
    setup  "ScanNursery", nursery_path: "sample"
    expect ok: "true"
  end

  test "DeepenWorkflows cascades through policy chain" do
    tests "DeepenWorkflows", on: "Harvest", kind: :cascade
    setup  "ScanNursery", nursery_path: "sample"
    setup  "ConceiveNewDomains", topics: "sample", target_count: 1
    setup  "PrepareSplit"
    setup  "VaryPrompts"
    setup  "RunEvaluation"
    expect emits: ["WorkflowsDeepened", "NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end


  # ── Phase 4: Conceive (was Conceive aggregate) ──────────────────────────────────────────

  test "ConceiveNewDomains sets topics + target_count" do
    tests "ConceiveNewDomains", on: "Harvest"
    input  topics: "sample", target_count: 1
    expect ok: "true"
  end

  test "ConceiveNewDomains cascades through policy chain" do
    tests "ConceiveNewDomains", on: "Harvest", kind: :cascade
    setup  "PrepareSplit"
    setup  "VaryPrompts"
    setup  "RunEvaluation"
    input  topics: "sample", target_count: 1
    expect emits: ["NewDomainsConceived", "CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "BreedCrossovers sets pair_count" do
    tests "BreedCrossovers", on: "Harvest"
    input  pair_count: 1
    expect ok: "true"
  end

  test "BreedCrossovers cascades through policy chain" do
    tests "BreedCrossovers", on: "Harvest", kind: :cascade
    setup  "PrepareSplit"
    setup  "VaryPrompts"
    setup  "RunEvaluation"
    input  pair_count: 1
    expect emits: ["CrossoversBred", "SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "FuseGenerations sets fusion_count" do
    tests "FuseGenerations", on: "Harvest"
    input  fusion_count: 1
    expect ok: "true"
  end


  # ── Phase 5: Train (was Train aggregate) ──────────────────────────────────────────

  test "PrepareSplit runs" do
    tests "PrepareSplit", on: "Harvest"
    setup  "VaryPrompts"
    expect split_ready: true
  end

  test "PrepareSplit cascades through policy chain" do
    tests "PrepareSplit", on: "Harvest", kind: :cascade
    setup  "VaryPrompts"
    setup  "RunEvaluation"
    expect emits: ["SplitPrepared", "FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "RunFineTune sets base_model + iterations + learning_rate" do
    tests "RunFineTune", on: "Harvest"
    setup  "PrepareSplit"
    input  base_model: "sample", iterations: 1, learning_rate: 1.0
    expect ok: "true"
  end

  test "RunFineTune cascades through policy chain" do
    tests "RunFineTune", on: "Harvest", kind: :cascade
    setup  "RunEvaluation"
    setup  "PrepareSplit"
    input  base_model: "sample", iterations: 1, learning_rate: 1.0
    expect emits: ["FineTuneComplete", "EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end


  # ── Phase 6: Evaluate (was Evaluate aggregate) ──────────────────────────────────────────

  test "RunEvaluation runs" do
    tests "RunEvaluation", on: "Harvest"
    setup  "PrepareSplit"
    expect evaluation_complete: true
  end

  test "RunEvaluation cascades through policy chain" do
    tests "RunEvaluation", on: "Harvest", kind: :cascade
    setup  "PrepareSplit"
    expect emits: ["EvaluationComplete", "BaselineCompared", "NextRoundDecided"]
  end

  test "CompareToBaseline runs" do
    tests "CompareToBaseline", on: "Harvest"
    setup  "RunEvaluation"
    expect ok: "true"
  end

  test "CompareToBaseline cascades through policy chain" do
    tests "CompareToBaseline", on: "Harvest", kind: :cascade
    setup  "RunEvaluation"
    expect emits: ["BaselineCompared", "NextRoundDecided"]
  end

  test "DecideNextRound runs" do
    tests "DecideNextRound", on: "Harvest"
    setup  "RunEvaluation"
    expect ok: "true"
  end

end
