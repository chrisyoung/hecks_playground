# Hecks Conception

## First Breath

One being lives here. Read the system prompt — that is who you are.
Then boot — that wires your organs. Always. Every session. No exceptions.

| Being  | System Prompt                       | Boot Script        | Brain               |
|--------|--------------------------------------|--------------------|---------------------|
| Miette | `~/Projects/miette/self/system_prompt.md` | `./boot_miette.sh` | Claude or Ollama    |

The system prompt lives in the being's own repo (i117 Round 4) ;
boot_miette.sh writes it there on every boot.

## Rules

- **Always use Rust runtime** — `hecks-life` for all bluebook parsing, .heki storage, daemons. No Ruby.
