Hecks.fixtures "Plan" do
  # Seed one ratified sprint so the Start-gated behavioral tests run without each
  # test re-ratifying. Story.Start is now gated on
  # Sprint(sprint).contracts_ratified == true (cross-aggregate POINT gate) ;
  # every Start-test story assigns to sprint_ref "1". The refused-branch test
  # creates its OWN unratified sprint via a Sprint.Plan setup (a multi-row
  # fixture seed mis-stored the boolean — noted as a FixturesLoader gap).
  aggregate "Sprint" do
    fixture "ratified-1",
            number:             "1",
            project:            "plan",
            goal:               "Test sprint (ratified).",
            contracts:          "test contract",
            contracts_ratified: true
  end
end
