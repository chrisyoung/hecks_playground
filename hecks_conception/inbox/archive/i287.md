---
ref: i287
status: done
priority: high
posted_at: '2026-04-30T13:45:00Z'
created_at: '2026-05-01T04:09:20Z'
updated_at: '2026-05-01T04:09:20Z'
value: substrate regenerates from meta-shapes — close the 2nd Futamura proof for the i122 layer itself
source: heki-archive-migration
---

# i287 — substrate regenerates from meta-shapes — close the 2nd Futamura proof for the i122 layer itself

substrate regenerates from meta-shapes — close the 2nd Futamura proof for the i122 layer itself

Today's i122 substrate ships as hand-written Rust across four files :

  - hecks_life/src/dispatch_query.rs (~350 LoC) — the IR-query substrate
  - hecks_life/src/validator_corpus.rs (~150 LoC) — the corpus-aware lint extensions
  - hecks_life/src/run_statusline.rs (~660 LoC) — statusline runner
  - hecks_life/src/run_boot/system_prompt.rs (~200 LoC) — boot Phase 4 runner

Each carries an inline antibody-exempt marker pointing at this arc as its retirement contract. They are kernel-surface today : they USE the IR but aren't yet emitted from a meta-shape that the IR can introspect.

The closing move — capability_runner_shape and similar meta-shapes already drafted in capabilities/ regenerate these files via the specializer. Acceptance for full self-validation :

  - capability_runner_shape.fixtures populated with rows for run_statusline, run_boot/*, run_status/*, the existing run_* family
  - capability_runner_shape's specializer emits run_*/mod.rs files byte-identically
  - dispatch_query gets its own meta-shape (or rolls into capability_runner_shape if the body_kind taxonomy fits)
  - validator_corpus extracted earlier today moves to a meta-shape too — the lint rules become declarative
  - the four files above land in the specializer golden test alongside dump.rs / validator.rs / validator_warnings.rs ; byte-identity actively enforced

When this lands, hand-edits to the substrate become structurally impossible (they fail the parity test), and the IR-query inversion is itself bluebook-derived. The substrate validates the substrate.

Sibling to i77 (validator-morphology-as-bluebook) and i78 (specializer-files-as-bluebook). i146 is the rollup arc that names the overall closing move : every kernel runner regenerable from a meta-shape.

PROGRESS 2026-04-30 — substrate wave landed in commits 5ef46b1d, df3e59c2, f0a69ed8. Five substrate files now byte-identical via specializer :
  - hecks_life/src/dispatch_query.rs           ← dispatch_query_shape
  - hecks_life/src/validator_corpus.rs         ← validator_corpus_shape
  - hecks_life/src/run_statusline.rs           ← statusline (extended)
  - hecks_life/src/run_boot/system_prompt.rs   ← system_prompt_assembly
  - hecks_life/src/runtime/repository.rs       ← repository_shape (i147 spillover)

Cumulative byte-identity coverage : 12 specializer goldens (was 6 at session start). Hand-edits to any tracked file fail the parity test by design.

Pattern that emerged : verbatim_section body_kind. Each Section row points at a .rs.frag snippet ; emitter concatenates by order. Cleaner than dump.rs's elaborate json_object/embedded_helper/enum_match taxonomy when the target's methods are sui-generis Rust.

NEXT WAVE (i147 follow-on) : behaviors_fixtures.rs, conceiver/generator.rs, run_boot/discover.rs — agents running. After those, the TRACTABLE tier from /tmp/i147_kernel_survey.md continues.

Status stays in_progress until i146's full scope (the substrate self-validates) is complete.

CLOSED 2026-04-30. All 5 substrate files now byte-identical via specializer goldens : dispatch_query, validator_corpus, run_statusline, run_boot/system_prompt, runtime/repository. Hand-edits structurally fail the parity test. Substrate self-validates. Closes the 2nd Futamura proof for the i122 layer. Sibling i77 / i78 / i26 remain open as the broader autophagy arc.
