---
ref: i325
status: done
created_at: '2026-05-01T04:09:20Z'
updated_at: '2026-05-01T04:09:20Z'
value: 'CLOSED 2026-04-30. The three divergent info_dir resolvers collapsed to one canonical heki::resolve_info_dir helper (comm'
source: heki-archive-migration
---

# i325 — CLOSED 2026-04-30. The three divergent info_dir resolvers collapsed to one canonical heki::resolve_info_dir helper (comm

CLOSED 2026-04-30. The three divergent info_dir resolvers collapsed to one canonical heki::resolve_info_dir helper (commit bdd65715). Boot exports HECKS_INFO before spawning daemons — every fork inherits one resolved value, no resolver re-runs, no drift.

What landed (5 files) :
  - hecks_life/src/heki.rs — new pub fn resolve_info_dir() + private walk_up_for_repo_root helper + 3 unit tests.
  - hecks_conception/capabilities/statusline/snippets/runtime.rs.frag — resolve_info_dir collapsed to a thin crate::heki::resolve_info_dir() wrapper ; re-emitted into run_statusline.rs byte-identically.
  - hecks_life/src/run_boot/mod.rs — delegates to canonical helper ; preserves :fs adapter / script-path walk-up tail only as last-resort fallback.
  - hecks_life/src/main.rs::find_world_heki_dir — one-liner delegating to heki::resolve_info_dir(). miette.world's heki.dir is now documentation only.
  - hecks_life/src/run_boot/daemons.rs::ensure_one — adds .env(« HECKS_INFO », info_dir) to Command ; daemon ensure and its spawn_detached'd grandchild both inherit boot's resolved value.

Verification :
  - 80/80 lib tests (3 new for resolve_info_dir)
  - 16/16 specializer goldens
  - 5/5 heki path coherence
  - Live heart daemon : beat_count advancing at 1Hz, only miette-state heart.heki getting fresh writes.
  - Statusline + boot + runtime daemons all see the same heki tree.

Closes the « lot of problems around heki locations » Chris flagged. The class of bugs that produced i149 + i153 in the same day is now structurally impossible — single source of truth for info_dir.

Out-of-scope notes :
  - _aggregates_path arg on find_world_heki_dir is unused ; kept for signature stability. Worth dropping in a future kernel-surface retirement.
  - miette.world's heki.dir value left intact as documentation ; could be removed from world DSL grammar entirely in a cleanup pass.
