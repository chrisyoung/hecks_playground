---
ref: i115
status: queued
priority: medium
value: 'Breadcrumb deserves its parameters AND its cascade. Today the statusline shows `🛠️ Inbox.Add`. Two upgrades: (a) include the args humans care about'
posted_at: 2026-04-28
source: heki-migration
---

# i115 — Breadcrumb deserves its parameters AND its cascade. Today the statusline shows `🛠️ Inbox.Add`. Two upgrades: (a) include the args humans care about

Breadcrumb deserves its parameters AND its cascade. Today the statusline shows `🛠️ Inbox.Add`. Two upgrades: (a) include the args humans care about — `🛠️ Inbox.Add ref=i112` — drawn from dispatched attrs (skip lists/maps/null, ≤32-char value cap, ≤80-char total args width, sorted scalar keys); (b) show the cascade chain when one dispatch triggers others, single line: `🛠️ EnterSleep → ElapsePhase → AdvanceLightToRem` with truncation when chain > 4 (first-2 + ellipsis + last-1). The dream's JOINED: insight rendered onto the statusline. Implementation: thread a cascade Chain through Runtime::dispatch + drain_policies; write to .last_dispatch on top-level exit; suppress when HECKS_DAEMON=1. Detail design + 3 paths in agent block report (path 1=register exemption ships now; path 3=full bluebook is the right end-state).
