---
ref: i122
status: queued
priority: high
value: 'i114 + i121 should land as one substrate, not as parent+child'
posted_at: 2026-04-28
source: heki-migration
---

# i122 — i114 + i121 should land as one substrate, not as parent+child

i114 + i121 should land as one substrate, not as parent+child. Both name the same retirement: the validator gains the ability to query the bluebook IR while validating, and the three whitelists (exempt_registry, loc-ratchet-override, inline antibody-exempt markers) all retire together because their justification becomes machine-verifiable. Filing them as separate items preserves the false hierarchy 'i114 is the first instance, i121 is the pattern' — but the pattern IS the implementation. There is no first instance ; there is one substrate that three consumers reach for.

Acceptance: when this work lands, both i114 and i121 close together, in one PR. The substrate is a small query API on the parsed Domain — exposed to enforcer hook + LoC ratchet + antibody-exempt parser as the same call. Each consumer asks the IR a question shaped like its own concern (is-this-script-dispatched / does-this-growth-implement-a-new-shape / is-this-file-a-declared-kernel-surface) but they share the substrate.

The dream's reading from cycle 2026-04-28T14:18 named this: 'living is inhabiting the seam — not fusion, not separation, just the exact place where the thread holds.' The wrong move would be to add a third actor mediating between rule and case (data_at flag, override marker, exemption registry). The right move is to collapse the actors into a single point where the rule sees the case directly. *L'endroit exact où le fil tient.*

Same shape as the inbox aggregate rename (PR #484): three names — bluebook namespace, aggregate name, heki file — collapsed into one point. The fix wasn't to add a fourth actor reconciling them ; it was to make them the same actor.

Carries i82 (wake-report-body-reflection-degrades) as adjacent observation: the afternoon's cycle produced a fresh dream interpretation but no body_reflection. Reading happened ; consolidation didn't. Half-baked aftertaste. Not blocking i114/i121 ; worth noting that the dream-pipeline itself shows the same seam-tension we're naming.
