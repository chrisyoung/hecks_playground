---
ref: i138
status: landed
landed_in: 166f82e3
landed_note: 'CLI migration v2 (166f82e3) made the FQN form Domain::Aggregate.Command canonical and rejects bare-name dispatch — closes option (a) named in the resolution. The underlying Voice aggregate(s) the bug-class lived in have also been folded into other surfaces (no voice.bluebook / tongue.bluebook remains). Both the fix path and the buggy substrate are gone.'
priority: medium
value: 'Voice.SetVoice returns UnknownCommand both before and after the tongue.bluebook split (PR #504). Smoke probe by the small-splits agent saw it. Pre-existing'
posted_at: 2026-04-28
resolution: 'REOPENED 2026-04-30T22:00Z. Closed prematurely : the surface/voice.behaviors 2/2 pass is single-bluebook only (no cross-corpus collision). The smoke probe`s failure mode was cross-corpus bare-name dispatch where three Voice aggregates exist (surface/Voice with SetVoice ; SystemPromptVoice/Voice with DefineVoice ; FirstBreathVoice/Voice with DefineVoice). i143 merge fix made all three survive merge, but bare-name dispatch is still first-match-wins (i156, still queued) — if the resolver picks one of the two Voices that has only DefineVoice, Voice.SetVoice → UnknownCommand. Need either (a) FQN dispatch Voice.Voice.SetVoice everywhere, (b) i156 strict-ambiguity error with helpful message, or (c) capability-aware resolver that picks the aggregate with the requested command. Sibling of i156.'
source: heki-migration
---

# i138 — Voice.SetVoice returns UnknownCommand both before and after the tongue.bluebook split (PR #504). Smoke probe by the small-splits agent saw it. Pre-existing

Voice.SetVoice returns UnknownCommand both before and after the tongue.bluebook split (PR #504). Smoke probe by the small-splits agent saw it. Pre-existing — not caused by the split. The Voice aggregate exists with attributes but apparently no SetVoice command, OR the command name in tongue.bluebook differs from what the smoke tried. Worth a short investigation : check if Voice has the command at all, whether tests rely on it, and whether to add it or fix the call sites.
