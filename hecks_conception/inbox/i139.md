---
ref: i139
status: landed
priority: medium
posted_at: '2026-04-28T21:24:31Z'
value: 'Mood.Express dispatch returns ''MissingAttribute(self-referencing id)'' even when name=mood is passed. Mood is identified_'
source: miette-state-migration
landed_in: cc7e169e
landed_at: 2026-05-12
landed_note: 'Resolved by the universal-id self-ref fallback : `reference_to(AggName)` now accepts both `snake_case(AggName)=<id>` AND the universal `id=<value>` form, plus the no-reference_to natural-key path (`name=mood` for an identified_by :name aggregate) works through the same mechanism. Previously closed in de7003ea citing i143/i151 ; tonight''s sidequest cc7e169e closes the dispatch path for real.'
---

# i139 — Mood.Express dispatch returns 'MissingAttribute(self-referencing id)' even when name=mood is passed. Mood is identified_

Mood.Express dispatch returns 'MissingAttribute(self-referencing id)' even when name=mood is passed. Mood is identified_by :name, and Express has no reference_to clause, so the runtime should accept name= as the natural-key kwarg. The dispatch loop's logic in command_dispatch.rs ought to use repo.id_for_command for the no-reference_to case. Either the runtime path has a regression or Mood needs explicit reference_to(Mood) to make self-reference resolution work. Either way the body of Mood.Express was preserved verbatim across the i112 Round 2c body split (PR #499), so this is pre-existing. Reproduction : HECKS_INFO=/tmp/probe-info hecks_life/target/release/hecks-life aggregates/ Mood.Express name=mood state=excited.
