---
ref: i142
status: queued
priority: high
value: '(SHIPPED Tier 1 via PR #515'
posted_at: 2026-04-29
source: heki-migration
---

# i142 — (SHIPPED Tier 1 via PR #515

(SHIPPED Tier 1 via PR #515 — refiled because the original i142 row was lost in merge-ordering before this re-filing.) Bluebooks ARE bounded contexts. Runtime now resolves Context.Aggregate.Command (3-part), Aggregate.Command (2-part), and bare Command (legacy) addresses. PR #515 plumbed context: Option<String> through ir.rs, parser.rs sets it from Hecks.bluebook namespace, command_dispatch::resolve dispatches by full path, main.rs CLI stops stripping the prefix. Tiers remaining: Tier 2 (heki path migration to <context>/<aggregate>.heki), Tier 3 (DSL cross-context references, context_map declarations — i134 substrate), Tier 4 (deprecation of bare form when ambiguous). Plus dump.rs + canonical_ir.rb should emit context for parity round-trip honesty. The Tier 1 work that retired i140 is shipped; further cleanup of namespace-rename workarounds (AwarenessMoment, MietteMemory, NurseryCorpus, BootSequence) is a separate follow-up that depends on the corpus actually using Context.Aggregate.Command in dispatch sites — the runtime accepts it, but most call sites still pass Aggregate.Command form.
