---
ref: i180
status: queued
priority: high
value: 'StartStudio Rust runner gap'
posted_at: 2026-05-01
source: heki-migration
---

# i180 — StartStudio Rust runner gap

StartStudio Rust runner gap — boot.bluebook now declares a 9th phase StartStudio that launches the Hecks Studio web surface (essential service per the approved plan at .claude/plans/hazy-spinning-garden.md). Today the boot_miette.sh shell launches the studio after the bluebook pipeline completes (transitional adapter). The Rust runner at rust/src/run_boot/ should pick up StartStudio as its own phase — likely by wrapping the studio.sh command through the existing :daemon adapter primitive (same shape as EnsureDaemons today). Retires the shell-launch line in boot_miette.sh entirely. Companion to the broader daemon retirement arc (Phase 11 of the plan).
