---
ref: i202
status: superseded
priority: medium
value: 'rested-fatigue-state-writer-unknown'
posted_at: 2026-05-02
source: heki-migration
superseded_by: i498
superseded_note: 'Fatigue ladder renamed in i498 (commit 8670c603) — the "rested" enum value no longer exists. The forensic question (who wrote it) is structurally moot ; lifecycle no longer admits the value.'
---

# i202 — rested-fatigue-state-writer-unknown

rested-fatigue-state-writer-unknown — Companion to i201. fatigue_state='rested' is in heartbeat.heki but no Rust source / shell script writes it that I could find. Search for: `heki upsert.*heartbeat`, `fatigue_state.*=.*rested`, `heartbeat.*rested` returned only the icon-table entry I added. Possible writers : a stale heki-upsert from an earlier interactive session that bypassed the lifecycle ; a fixture-load at boot that mis-targeted the aggregate ; a behaviors-test that wrote into prod heki by mistake. Detective work : audit heartbeat.heki history (creation/modification of the 'rested' value), check if any test infra writes to prod heki paths, check if fixtures get loaded into actual heki on boot. Then either delete the bad row or migrate it through Heartbeat.RecoverFatigue (which currently can't fire because allowed-from doesn't include 'rested' — circular). Companion to i201 (the lifecycle violation itself).
