---
ref: i217
status: queued
priority: high
value: 'i221-chain-not-firing-in-canonical-dispatch'
posted_at: 2026-05-03
source: heki-migration
---

# i217 — i221-chain-not-firing-in-canonical-dispatch

i221-chain-not-firing-in-canonical-dispatch — the LlmDispatcher Rust port (PR #579, merged) has 8 integration tests passing but the auto-fire-on-dispatch chain (Runtime::dispatch → resolve_llm_adapters → ClaudeProvider → dispatch_cascade) does not actually trigger when invoked from CLI against canonical hecksagons. Verified : hecks-life $AGG Dream.RecordImage text_fr='trigger probe' returns text_fr='trigger probe' — if the chain fired, the :dream_image adapter (response_into_target='Dream.RecordImage') should have substituted its prompt template, called Claude, and chained dispatch_cascade(Dream.RecordImage, text_fr=<response>) — overwriting the test value with Claude's output. Diagnosis paths : (a) check if register_llm_providers actually populates self.llm_providers under the dispatch_hecksagon path, (b) check if the agent's matching criterion (response_into_target == AggCmd) is correct semantically — Ruby Phase 2 uses EXPLICIT runtime.llm(:adapter_name, attrs) invocation, not auto-trigger ; the agent invented an auto-fire that may or may not match intent, (c) verify the chain dispatch actually persists. Adjacent : i75 dream-shell retirement depends on this chain working ; until it does, rem_branch.sh's claude -p direct call can't retire.
