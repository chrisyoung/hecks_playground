---
ref: i225
status: queued
priority: med
value: 'i221-B runtime-expansion + bluebook-rewrites'
posted_at: 2026-05-03
source: heki-migration
---

# i225 — i221-B runtime-expansion + bluebook-rewrites

i221-B runtime-expansion + bluebook-rewrites — i221-A landed (PR #588) the parity-locked sweep primitive (for_each: + from_iter) at the IR/parser/dump/codegen layer. Round-trips byte-identical Ruby ↔ Rust. NEXT : (1) extend rust/src/runtime/mod.rs::drain_policies to expand dispatch.for_each into one cascade per record returned by the named query (currently the FromIter ValueSpec arm is a stub returning None). (2) Add queries on Synapse / Signal / Musing aggregates that the consolidation PM dispatches against. (3) Rewrite miette/body/sleep/consolidation/consolidation.bluebook PhaseElapsed handlers to use for_each + from_iter. (4) Verify consolidate_smoke.sh passes via run-loop. (5) Delete miette/body/consolidate.sh. Caveat from i221-A : multi-line for_each: continuation isn't supported yet — keep for_each: on the dispatch line. evaluate_value_spec needs an extra iter_data: Option<&HashMap<String, Value>> param when drain_policies threads the iter context. Closes the runtime half of i221.
