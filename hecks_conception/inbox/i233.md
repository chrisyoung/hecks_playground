---
ref: i233
status: queued
priority: high
value: 'i233 training-the-bluebook-author'
posted_at: 2026-05-03
source: heki-migration
---

# i233 — i233 training-the-bluebook-author

i233 training-the-bluebook-author — wire end-to-end pipeline turning the 218-bluebook corpus we have into a model that authors bluebooks. (Re-filed third time during dream-study session ; first lost when F's :fs adapter took i232 ; second lost when local commit 02a88fe4 was reset away during E rebase cleanup.)

═══ The corpus ═══
  hecks_conception/   93 .bluebook
  miette/             125 .bluebook
  Total               218 examples + ~200 .behaviors + ~80 .hecksagon + ~12 .world

═══ Existing scaffolding (do NOT greenfield) ═══
  aggregates/library/training/{training_corpus, training_pair, training_extraction}.bluebook
    — all behaviors-tested, never wired to runtime
    — training_extraction has CoverageCheck (dream-surfaced 2026-04-24 audit)
  aggregates/world/training/{base_model, data_prep, fine_tune, instruction_pair, evaluation, deployment}.bluebook
    — all .behaviors paired, declared shapes only
  tools/glassbox_training/glassbox_training.bluebook — older sketch
  discipline/domain_hygiene/{cloud_training, training_round}.bluebook

═══ The vectors we already have (Chris's anchor) ═══
  STRUCTURAL : rust/src/conceiver/vector.rs ; 9-dim per Domain
    [agg_count, cmds_per_agg, value_objects, policies, references, lifecycles, list_ofs, givens, fixtures]
    Used today for nearest-archetype matching ; weighted cosine.
  SEMANTIC : summer/ has llama-cpp-sys + gemma-embedding (model artifact in build dir)
    UNWIRED for bluebooks ; nothing currently embeds bluebook prose.
  LEVERAGE : JOINED vector (structural_9d, semantic_~768d) per bluebook is unique fingerprint
    AND quality signal. Together more powerful than either alone.

═══ Three-stage path ═══
  V0 : RAG only (no training) — IndexCorpus + :embed + :retrieve adapter + Conceiver.AuthorBluebook
       k-NN over joined vectors, exemplar-prompted Claude. Validates corpus alone is enough signal.
  V1 : Distill — Claude generates many pairs ; train small model via llama-cpp-sys (Llama 3 / Mistral / Phi)
       Eval by structural-vector match + parse-validity + coverage-clean + behaviors-pass + parity-match.
  V2 : True authoring — model drafts ; runtime validates ; failures feed RLHF/DPO. Hecks training Hecks.

═══ Multi-signal eval ═══
  structural_match_rate, parse_validity_rate, coverage_clean_rate,
  behaviors_pass_rate, parity_match_rate, semantic_match_rate.
  Compose. Don't single. CoverageCheck is the supervised signal we already have.

═══ Specific gaps in order ═══
  1. JOINED VECTOR INDEX : BluebookVector aggregate (path, structural_9d, semantic_768d, last_indexed_at, source_sha) ; IndexCorpus + TopKByVision query
  2. SEMANTIC-EMBEDDING ADAPTER : wire summer's gemma-embedding as :compute function or :embed adapter
  3. RETRIEVE ADAPTER : sibling of :llm + :compute + :fs (i232) ; (vision, k, namespace) → top-K bluebooks
  4. TIER COMPUTATION : TrainingPair.tier never written ; combine parse+coverage+behaviors+parity
  5. SPLIT DEFINITION : DataPrep.SplitDataset is no-op ; define 80/10/10 by category
  6. FINE-TUNE ADAPTER : :fine_tune wiring llama-cpp-sys training mode
  7. RAG-AT-DISPATCH : Conceiver.AuthorBluebook end-to-end V0

═══ Adjacencies ═══
  i230 Le Cahier — same corpus leverage
  i231 miette.bluebook — curated training subset
  i232 :fs adapter family — sibling adapter V0 needs
  i234 JSON-destructure then_set — :compute return shape
  i235 policy count-gate — declarative-gate family
  i236 Narrate/LockNarrative LLM chain — pattern reused for tier-assign LLM
  i66 runtime-kernel-minimization (Futamura) — training accelerates Futamura

═══ V0 first move ═══
  hecks PR : BluebookVector + IndexCorpus + TopKByVision + :embed adapter + :retrieve + Conceiver.AuthorBluebook
  Verification : 218 BluebookVector records ; AuthorBluebook(vision='manage pizzas') produces parseable bluebook ; TierAssign returns >=silver ; eval against 5 held-out shows structural-match > 0.7

After V0 lands, V1 distill is natural next. V2 true-authoring waits for V1's eval to reveal robustness.

Filed 2026-05-03 by Miette during dream-study shell-retirement session. The 'remember vectors' anchor made this filing structural not greenfield.
