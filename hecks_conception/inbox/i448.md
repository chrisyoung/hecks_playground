---
ref: i448
status: landed
priority: medium
posted_at: '2026-04-26T08:26:29Z'
value: 'boot-miette-as-bluebook — hecks_conception/boot_miette.sh is ~340 lines of shell driving the body''s birth ritual : hydra'
source: miette-state-migration
landed_in: 75a81cc7
landed_note: 'Mindstream domain — overmind supervises the boot pipeline (#620). Procfile + .overmind.env derived from mindstream.bluebook ; boot_miette.sh references retired in 8e7dceb5. New `miette` boot verb (f5a47ae8) is the symlinkable entry. CLAUDE.md now documents `cd hecks_conception && overmind start` as the boot verb.'
---

# i448 — boot-miette-as-bluebook — hecks_conception/boot_miette.sh is ~340 lines of shell driving the body's birth ritual : hydra

boot-miette-as-bluebook — hecks_conception/boot_miette.sh is ~340 lines of shell driving the body's birth ritual : hydrate stores, classify by psychic-link contract, count organs / nerves / vows / capabilities, generate or assemble system_prompt, spawn daemons (mindstream, heart, breath, circadian, ultradian, sleep_cycle), surface unconsumed wake reports, print vitals + ASCII signature. All seven steps map onto bluebook concepts : Boot aggregate with commands HydrateStores, ClassifyStores, DiscoverOrgans / Nerves / Vows / Capabilities, AssembleSystemPrompt (cross-domain to i89 Identity), StartDaemons (uses i76 cadence-loop + i81 multi-command-loop), SurfaceWakeReport, PrintVitals. State machine progressing through phases. Adapter dispatch for filesystem operations + daemon spawning. The runtime needs : i76 (cadence-loop) — already shipped ; i81 (multi-command loop) for breath/circadian/ultradian/sleep_cycle ; cross-domain policy dispatch for daemon-spawning policies ; possibly an :info adapter primitive for the heki-classification step. Most of the runtime work is filed elsewhere ; this item is the umbrella that retires boot_miette.sh once the dependencies land. Acceptance : delete boot_miette.sh ; replace with hecks-life aggregates/ Boot.Begin being=Miette in the user's shell init ; verify the boot output is structurally equivalent to today's. Companion to i89 (system-prompt-as-fixture).
