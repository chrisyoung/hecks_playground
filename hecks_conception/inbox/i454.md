---
ref: i454
status: landed
priority: low
value: 'Post-Phase-E orphaned Rust files: duplicate_policy_validator.rs (86 LoC) and lifecycle_validator.rs (288 LoC). These wer'
source: miette-state-migration
landed_in: 181b4feb
landed_note: 'i147 W9-C — rust/src/lifecycle_validator.rs retired via lifecycle_validator_shape (PR #557). Companion duplicate_policy and diagnostic_validator orphans absorbed by the i147 arc.'
---

# i454 — Post-Phase-E orphaned Rust files: duplicate_policy_validator.rs (86 LoC) and lifecycle_validator.rs (288 LoC). These wer

Post-Phase-E orphaned Rust files: duplicate_policy_validator.rs (86 LoC) and lifecycle_validator.rs (288 LoC). These were generated by Ruby's DiagnosticValidator thin-subclass pattern (DuplicatePolicy, Lifecycle). Phase D did not port a Rust diagnostic_validator specializer. Phase E deleted the Ruby subclasses. The .rs files are now frozen relative to their shapes — any change to duplicate_policy_validator_shape or lifecycle_validator_shape cannot be regenerated; the .rs would drift.

Work: port Ruby DiagnosticValidator (153 LoC, deleted but recoverable via git history at f717b324~1) as Rust specializer hecks_life/src/specializer/diagnostic_validator.rs. Takes 1 DiagnosticValidator row + N DiagnosticHelper rows + report_kind dispatch (flat / flat_with_strict / partitioned_with_strict) + emit_helper + emit_rule. Thin subclasses become config structs passed to the base emit: DuplicatePolicyConfig { shape_path, target_rs }, LifecycleConfig { ... }, ValidatorWarningsConfig (already ported).

Once ported, dispatcher adds arms for duplicate_policy + lifecycle, golden tests expand to 9+, and the two orphaned files become regenerable. Estimate 3-4 hours.

Not urgent — these files don't change often. But the tracker notes the gap, and full autophagy parity means every retired .rs file has a Rust specializer.
