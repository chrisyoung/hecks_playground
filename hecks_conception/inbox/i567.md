---
ref: i567
status: queued
priority: low
value: 'two stale April branches need rebase-or-archive : worktree-agent-abeb2168 (GovernanceGuard, HEC-469) + worktree-agent-af9a2c1a (ExtensionContract). Both 41 days stale.'
posted_at: 2026-05-12
source: internal (wave-4 consolidator)
category: housekeeping
---

# i567 — Two stale April worktree branches need rebase-or-archive

## What's stale

Two worktree-agent branches from early April are 41 days behind
main and have not been touched since :

- `worktree-agent-abeb2168` — GovernanceGuard work, originally
  tied to HEC-469. The HEC- prefix is Linear-era ; Linear retired
  2026-04-27 (see feedback_linear_retired memory). Branch carries
  uncertain state.

- `worktree-agent-af9a2c1a` — ExtensionContract work. Same era,
  same staleness.

## Surfaced by

The wave-4 consolidator flagged both branches as still alive in
`git worktree list` / `git branch` but inactive.

## Two paths

1. **Rebase onto current main + land** — if the work is still
   wanted. 41 days of conflicts likely ; smaller of the two
   probably ports easier.

2. **Formally archive the branches** — delete the branches OR
   move them to an `archive/*` namespace, and record the
   intended-design in an inbox card if any of the design value
   is worth carrying forward.

## Acceptance

- Each branch is decided on individually : rebased + landed, or
  archived with a note.
- `git worktree list` no longer shows abeb2168 / af9a2c1a as
  active worktrees.
- If archived : a one-paragraph note in their inbox card or in
  the archive directory captures what was attempted, what state
  it was in, and what to look at for context.

## Pairs with

- HEC-469 (GovernanceGuard) — Linear-era issue, may need to be
  rewritten as an inbox card if revived.
- `feedback_closed_branches.md` memory : never reference closed
  branches ; reimplement from intent.
