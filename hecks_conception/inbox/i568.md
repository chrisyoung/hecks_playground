---
ref: i568
status: landed
priority: medium
value: 'Lifecycle-tracked attributes wrapped as VOs must declare `default:` matching the lifecycle''s initial state. Surfaced 5× tonight. macrophage check should flag missing defaults.'
posted_at: 2026-05-12
landed_in: 248e9043
landed_note: 'New check `check_lifecycle_attr_missing_default` added to the lifecycle_validator (rust/src/lifecycle_validator.rs + codegen sources at codegen/lifecycle_validator_shape/). Fires error severity when a `lifecycle :foo` block tracks an attribute whose type is a VO declared on the same aggregate AND the attribute has no `default:` clause. Three new tests in rust/tests/lifecycle_validator_test.rs (synthetic regression case, positive case with default, primitive-typed pass-through). Corpus sweep surfaced 9 existing bluebooks failing the new check — these are real bugs (see Pairs-with). Wired into `storehouse check-all` and `check-lifecycle` paths so the pre-commit hook catches future regressions.'
source: internal (cli/integrations sweep `3d9c61b3` + runtime/ sweep `8f927a58`)
category: discipline
---

# i568 — Lifecycle-tracked VO must carry `default:` matching initial state

## What this card carries

When an aggregate's lifecycle tracks an attribute that is wrapped
as a value object, the VO MUST declare a `default:` clause whose
value matches the lifecycle's initial state. Otherwise the
dispatcher (see i563) has nothing to return on readback before
the first transition, and you get `'[0 items]'` instead of the
expected scalar.

## Surfaced 5× tonight

- **Console.Speaker** — `status` lifecycle, wrapped as
  `SpeakerStatus` VO, no `default:` (cli/integrations sweep
  `3d9c61b3`).
- **Cli.Invocation** — `phase` lifecycle, wrapped as
  `InvocationPhase`, no `default:`.
- **Page** — `status` lifecycle, wrapped as `PageStatus`, no
  `default:`.
- **Generation** — `status` lifecycle, wrapped as
  `GenerationStatus`, no `default:`.
- **TrainingPair** — `coverage_status` lifecycle, wrapped as
  `CoverageStatus`, no `default:` (runtime/ sweep `8f927a58`).

Each had to be patched ad-hoc by the agent that hit it.

## Fix shape — macrophage check

A new macrophage check (let's call it
`lifecycle_tracked_vo_missing_default`) :

For every aggregate :
  For every `lifecycle :foo` block :
    Let `attr` = the aggregate attribute that `:foo` tracks.
    If `attr.type` is a VO :
      Require `attr.type` to declare `default: '<initial-state>'`
      where `<initial-state>` is the lifecycle's first
      `transition_from :nil` (or the documented initial state).
    Else (primitive attr) :
      No check.

## Acceptance

- Macrophage check fires on any new lifecycle-tracked VO without
  a matching `default:` clause.
- Check has a clear error message naming the attribute, the VO,
  and the expected default value.
- The 5 sites listed above either already have the default
  (landed in `3d9c61b3` / `8f927a58`) or are caught by the new
  check on re-run.
- Doc comment + behaviors test for the check itself.

## Pairs with

- **i563** — the runtime support side. i563 is the kernel making
  VO lifecycle attributes work ; i568 is the discipline check
  making sure they're declared correctly.

## Corpus sweep — existing bluebooks failing the new check (2026-05-12)

Nine pre-existing bluebooks fail the new check ; each is a real
bug (the lifecycle attr deserializes as `'[0 items]'` at runtime
and every `from:` clause fails). Fix per file = add the matching
`default:` clause to the wrapped attribute. Filed for the agents
that touch each :

- `tools/project_management/project_management.bluebook` —
  Feature.status, Sprint.status, Dependency.status,
  Milestone.status
- `tools/actions/actions.bluebook` — ActionPlan.status
- `tools/inventions/inventions.bluebook` — Invention.status
- `codegen/conception/conception.bluebook` —
  BluebookConception.phase, BehaviorsConception.phase,
  Development.phase
- `runtime/storage/heki.bluebook` — HekiFile.phase
- `runtime/storage/heki_r2.bluebook` — HekiR2Object.phase
- `runtime/query/query.bluebook` — Query.phase
- `ruby/hecks/appeal/hecks/workbench.bluebook` —
  Session.connection
- `ruby/hecks/appeal/hecks/collaboration.bluebook` —
  Feature.lifecycle_state

## Open questions

- Should the check also enforce that ALL declared VO `default:`
  values appear as transitions in the lifecycle ? Possibly a
  second check, possibly a fold of this one.
