---
ref: i638
status: open
priority: high
posted_at: 2026-05-16
posted_by: Miette (i80 Unit E deferred)
category: framework / i80 / structural
attached_to: hecks (cli/subcommand + runtime handlers)
value: 'Unit E : lift the run_loop/clock/run_pm_loop handler bodies into bluebook value-objects so the i630 FQN-truncation bug class becomes STRUCTURALLY impossible (not comment-guarded). Design is DONE (below). From-scratch impl, serialized E-0..E-4 ; deserves a fresh focused session, not a deep-context tail.'
---

# i638 — Unit E : handler bodies -> bluebook (E-0 .. E-4)

## Design (locked, from i2 restart card — verbatim)
Graft CadenceInvocation / CadenceInterval / GatePredicate /
ClockSegment value-objects onto the EXISTING SubcommandRegistry::
Subcommand aggregate. One single `parse_cadence_invocation` bridge.
run_loop / clock / run_pm_loop shrink to ~30-line shims with zero
string manipulation. NOT blocked on i218 (superseded by i516).
LoopDriver / tick-engine untouched. E-0 .. E-4 serialized. This is
the part that makes the i630 truncation class structurally
impossible rather than comment-guarded.

## Why deferred
Keystone i629 closed end-to-end + 8 clean commits banked this session.
Unit E is from-scratch domain+kernel design work (not WIP cleanup the
i2 card implied), multi-step, kernel-adjacent. Starting it at deep
context tail is the documented lost-hours pattern. i630 is already
functionally fixed + regression-locked (Unit B, commit 98a8458c) ;
Unit E is the STRUCTURAL hardening, valuable but not urgent.

## Closes when
run_loop/clock/run_pm_loop are ~30-line shims dispatching parsed
Cadence VOs ; no string-parsing of FQNs in handlers ; specializer
golden 29/0 ; keystone loop still fires.
