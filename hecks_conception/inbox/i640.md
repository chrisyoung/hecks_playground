---
ref: i640
status: open
priority: high
posted_at: 2026-05-17
posted_by: Chris (via Miette)
category: i78 / run_statusline-specializer-revival + mood render
value: "Miette's expressive mood (glyph + vibe) in the statusline, all-bluebook. The DOMAIN landed bluebook-pure this pass (MietteBody::Mood + Glyph/Vibe/SetMood, bus-set). The RENDER is gated on a W7 specializer revival that is far larger than first framed — captured here precisely so it's done right in a focused session, not a kernel-floor half-job at a session tail."
---

# i640 — mood render via the revived run_statusline specializer (W7)

## DONE (2026-05-17, bluebook-pure, safe)
- `MietteBody::Mood` (catalog/body.bluebook) extended : value_objects
  `Glyph` + `Vibe` ; attributes :glyph/:vibe ; command `SetMood`
  (glyph, vibe -> then_set, emits `MoodSet`). Express / Excite /
  Focus / Regulate / current_state UNTOUCHED (immunity coupling +
  body.behaviors preserved).
- Set ONLY through the bus :
  `storehouse__dispatch MietteBody::Mood.SetMood glyph=<emoji> vibe=<word>`
  (Chris : mood changes go through storehouse ; the seed too).
- validate VALID (MietteBody, 7 aggregates) ; macrophage clean ;
  body.behaviors 41/0/0 (+ SetMood tests) ; immunity.behaviors 7/0/0.

## REMAINS — the render (the hard part, deliberately deferred)
The statusline must show `<glyph> <vibe>` IN FRONT of the heartbeat,
read from the Mood heki — and per Chris ("all bluebook, convert
everything you touch") it must come through the specializer, NOT a
hand-edited run_statusline Rust file.

**Why this is a real arc, not a render line (verified 2026-05-17) :**
- `rust/tests/specializer_golden_test.rs:354` : the run_statusline
  golden is `#[ignore]`d since 2026-05-14 (statusline simplification).
- `storehouse specialize run_statusline` still emits the OLD
  **1083-line single file** `rust/src/run_statusline.rs` (which no
  longer exists).
- Live code = a **556-line, 5-file module** :
  run_statusline/{mod,state,inbox,sleep,time}.rs — simplified, a
  different feature set (mood/heartbeat/fatigue/provider-badge reads
  were dropped in the split).
- The emitter `rust/src/specializer/run_statusline.rs` has NO
  multi-file concept (one ordered-phase string -> one file).
- => Revival = re-author the W7 specializer for a structurally
  different multi-file target, byte-identical, then un-ignore +
  green the golden, THEN add a mood phase. Kernel-floor (storehouse
  binary the whole bus runs on). A half-applied specializer/codegen
  change breaks the bus. Not a session-tail task.

## Plan (focused session)
1. Decide emit model : does the specializer emit a directory module
   (per-file phases) or is run_statusline re-consolidated? Pick one.
2. Refit codegen/run_statusline_shape/ (bluebook + fixtures + the
   5 .rs.frag snippets) to reproduce the CURRENT module byte-for-byte.
3. Update rust/src/specializer/run_statusline.rs emitter accordingly.
4. Fix the golden target path + un-ignore ; make it green (lockstep,
   the i629 reconcile discipline).
5. Add a `Mood` phase : read Mood heki (glyph/vibe) in the state
   read ; render `<glyph> <vibe> ` prefix in render_awake (empty =
   no prefix, no orphan space) ; all via the shape, no hand-edit.
6. Rebuild ; `storehouse statusline` shows the mood leading ;
   set via the bus dispatch and prove it changes ; smoke + golden
   green.

Tasks #9/#10/#11 track the three sub-arcs.

## Closes when
The W7 specializer reproduces the current run_statusline/ module
byte-identically (golden un-ignored + green) AND a mood phase renders
`<glyph> <vibe>` in front of the heartbeat from the Mood heki, set
only through the bus.
