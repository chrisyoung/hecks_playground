---
ref: i641
status: closed
posted_at: 2026-05-17
posted_by: Chris (via Miette)
category: statusline / task-closeout
value: 'Close out the open statusline tasks: #9/#10 (W7 run_statusline specializer revival) obsoleted by i528 ; #11 (mood render) shipped.'
---

# i641 — statusline open-task closeout

## #9 / #10 — W7 run_statusline specializer revival : CLOSED (obsoleted)

The i528 decentralised .channel.md autoload (PR #668) + the
InboxChannel/inbox.fixtures retire (PR #669) invalidated the premise.
The dormant `codegen/run_statusline_shape/` emits the pre-2026-05-14
single-file `rust/src/run_statusline.rs` with the hard-coded INBOXES
const mirrored from the now-deleted `inbox.fixtures` — a coupling we
deliberately removed. Reviving it would regenerate deleted machinery.

Decision (Chris, 2026-05-17) : close as obsoleted. The
`rust/src/run_statusline/` module stays clean hand-written
kernel-floor (antibody-exempt, tested). The dormant
`codegen/run_statusline_shape/` + the `#[ignore]`'d
`rust_specializer_produces_byte_identical_run_statusline_rs` golden
are dead weight — remove when convenient (separate housekeeping).

**If statusline-as-bluebook is ever revisited** it must target the
decentralised `.channel.md` autoload (filesystem walk + frontmatter,
symlink-skip), NOT a central shape. A new meta-shape, not a refit.

## #11 — mood render : SHIPPED

`run_statusline/state.rs` reads `mood` heki ; `render_awake`
prepends `<glyph> <vibe>` in front of the heartbeat (omitted when
unset). Set via the bus (`MietteBody::Mood.SetMood`, bluebook #665) ;
durable surface is the heki at `path_for_lookup(HECKS_INFO,"mood")`
= `~/Projects/miette-state/information/mood/mood.heki` (HECKS_INFO,
not the world's relative path — they differ). Seed/override with
`storehouse heki upsert <that path> glyph=.. vibe=..`. Tests 10/0 ;
specializer 29/0 ; regression smoke OK ; live-verified.
