---
ref: i644
status: open
priority: high
posted_at: 2026-05-17
posted_by: Miette (surfaced building i638 Unit E — invariants are decorative framework-wide)
category: framework / kernel / invariant-engine
attached_to: hecks (ir_shape + parser_shape + runtime_shape + new evaluator)
value: 'VO `invariant "n" do <pred> end` blocks are parsed-and-DISCARDED conception-wide — never captured in IR, no evaluator, never enforced. inbox "non-empty"/"valid ref format", pizzas "amount must be positive", i638 ScheduledCommand "::" — all inert. Proven 2026-05-17 (two bus-routed probes: short-form FQN accepted with both verb- and value-bindings). This card is the COMPLETE, mechanical execution map so a fresh focused session builds the engine without re-discovering scope.'
---

# i644 — invariant enforcement engine (parser → IR → evaluator → runtime)

## Proven finding (2026-05-17)
`grep invariant rust/src` → only HTML *rendering* (`html_rules.rs`
collect_invariants for the domain UI) + incidental. **Zero in
ir.rs / parser.rs / validator.rs / runtime / interpreter.** The
parser accepts `invariant "n" do … end` inside `value_object`
generically and discards it; it never reaches the IR; nothing
evaluates it. Two bus-routed probes (RunCadence with
`verb:"BadNoColons"`, both `verb.` and `value.` invariant bindings)
both PASSED → the runtime does not reject short forms. VO invariants
are decorative everywhere.

## The DSL to support is TINY (entire conception, deduped)
```
value.length > 0
value.match?(/\Ai\d+\z/)
value.include?("::")         # i644/i638 : add to ScheduledCommand
                             # (pattern VALID-proven this session ;
                             #  i80 DispatchAddress precedent). i638
                             # finishes when this is ENFORCED here.
```
Three forms : `<bind>.length <cmp> <int>` · `<bind>.match?(/<re>/)`
· `<bind>.include?("<str>")`. `<bind>` ∈ { `value`, the attribute
name }. Build a BOUNDED evaluator for exactly these (+ `||`, `!`,
`.empty?` are cheap to add for headroom). NOT a Ruby interpreter.

## Exact edit map (all verified against origin/main 1b08a2d4)

### 1. IR — `ir_shape` (declarative, lowest risk, do first)
`codegen/ir_shape/fixtures/ir_shape.fixtures` :
- Add Type row (append after View order:36) :
  `fixture "Invariant", name: "Invariant", order: 37, body_kind: "struct", derives: "Debug, Clone", impl_target: "", body_snippet: "", doc_snippet: "", description: "Value-object invariant — name + predicate expression"`
- Add Invariant Field rows (with the other field-row group) :
  `fixture "Invariant_name",      type_name: "Invariant", order: 1, name: "name",      field_type: "String", doc_snippet: ""`
  `fixture "Invariant_predicate", type_name: "Invariant", order: 2, name: "predicate", field_type: "String", doc_snippet: ""`
- Add ValueObject field row (after ValueObject_attributes order:3) :
  `fixture "ValueObject_invariants", type_name: "ValueObject", order: 4, name: "invariants", field_type: "Vec<Invariant>", doc_snippet: ""`
Regen : `storehouse specialize ir > rust/src/ir.rs` ; golden
`rust_specializer_produces_byte_identical_ir_rs` (test:617). NOTE :
adding a struct field changes every `ValueObject { … }` literal in
the codebase → expect a compile cascade ; either add `..Default`
or update constructors (Default not derived — update constructors).

### 2. Parser — `parser_shape` (parser.rs is tracked, test:559)
`parser.rs:308` `else if line.starts_with("value_object")` →
`parse_value_object(&lines[i..])`. In that fn's body loop, before
the generic skip, recognise `invariant "<name>" do <pred> end`
(single-line form covers the whole conception) and push
`Invariant { name, predicate }` onto the VO. Edit the
`parser_shape` snippet that carries `fn parse_value_object`
(find: `grep -rl parse_value_object codegen/parser_shape/`),
regen `storehouse specialize parser > rust/src/parser.rs`, golden.

### 3. Evaluator — NEW module (untracked, no shape, low risk)
`rust/src/runtime/invariant_eval.rs` (≤~120 LoC) : `pub fn
holds(pred: &str, bind_name: &str, v: &Value) -> bool`. Parse the
3 forms by string match on `.include?(` / `.match?(` / `.length`.
Bind `value` AND the attribute name to the VO's scalar string.
Register `pub mod invariant_eval;` in runtime/mod.rs (runtime_shape
— see step 4, same regen).

### 4. Runtime hook — `runtime_shape` (mod.rs tracked, test ~646)
The VO coercion path (`runtime/mod.rs` ~1633 "coerces the resolved
Value to its type" + the command-input build path from `fn dispatch`
:252). After a VO value is built — RECURSING into nested VOs and
`list_of` elements — for each of the VO type's `invariants` call
`invariant_eval::holds` ; on false return a dispatch error
(`UnknownCommand`-style structured error, message
`"<VO> invariant '<name>' violated: <pred>"`). Edit the
`runtime_shape` fragment(s) for the coercion + the mod declaration,
regen `storehouse specialize runtime > rust/src/runtime/mod.rs`,
golden ~`rust_specializer_produces_byte_identical_runtime_rs`.

### 5. Acceptance + finish i638
- Probe (the discriminator) : RunCadence with
  `commands:[{verb:"BadNoColons"}]` must now **ERROR** (was: passed).
  Valid FQN + keystone `storehouse loop aggregates Inbox::Inbox.Check`
  still fire. behaviors 341/341. ALL specializer goldens green
  (ir/parser/runtime/cli_dispatch + 26 others). ratchet : net
  change should be ~0 (fixtures + fragments are IR-claimed ; the
  new evaluator module is the only raw .rs — ≤120 LoC, may need a
  per-instance loc-ratchet-override : Chris's call, NOT pre-written).
- Then **finish i638** : with the bluebook invariant now ENFORCED,
  delete the ~210-line hand-written Rust mirror + bridge from
  `codegen/cli_dispatch_shape/snippets/sec_04_helpers_main_block.rs.frag`
  (commit a7abf271's additions), thin run_loop/run_clock to
  pure-bluebook shims (parse argv → attrs → dispatch ; runtime
  validates incl. the `::` invariant), regen cli_dispatch. Net
  cli_dispatch LoC goes strongly NEGATIVE → the +68 ratchet block
  resolves by deletion, no override. i638 then closes truly
  bluebook-first with zero duplicative Rust.

## Why a card not done inline
Surfaced at the deep tail of an enormous session. It is a 4-tracked-
shape kernel feature (ir + parser + runtime + cli_dispatch regens) ;
each fragment→regen→byte-identity. Building it at this depth is the
documented lost-hours / parity-gate-corruption pattern. Zero engine
LoC landed despite full scoping — that is itself the signal. This
card makes the fresh session MECHANICAL : every edit point, the
exact fixture rows, the bounded DSL, the acceptance probe, and the
i638-finish are specified. Do steps 1→5 in order ; each is
independently gate-verified ; commit per step (split by concern on
ratchet calls — the evaluator module is the only ratchet-relevant
concern).

## Closes when
The RunCadence short-form probe ERRORS via the bluebook invariant ;
all specializer goldens green ; behaviors 341/341 ; keystone fires ;
i638 finished pure-bluebook (Rust mirror deleted) ; i638 + i644
archived.
