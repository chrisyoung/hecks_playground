---
ref: i645
status: open
priority: high
posted_at: 2026-05-17
posted_by: Miette (end-of-session restart prompt)
category: session-restart
value: 'Restart prompt for the next session. Carries PR #674 state + the i644 invariant-engine arc (the bluebook-first finish of i638) + the queued mod.rs bluebook-breakout. STEP 0 IS A FRESHNESS-CHECK (i643) : this session proved 3x that carded premises rot when origin/main moves. Verify before executing.'
---

# i645 — Restart prompt (2026-05-17 end of session)

## Paste this verbatim into the next session

```
Resuming after the i637/i638 session. Route EVERY tool call through
Storehouse : Tools::ShellTool.Bash / FileTool.* via storehouse__dispatch
(aggregates_dir = the hecks_conception of whatever checkout you are in).
FileTool.Write is broken — write files via ShellTool.Bash heredoc.
Never native Bash/Write/Edit unless byte-exactness on a Gate-9 surface
genuinely requires it, and say so out loud if you do. No Co-Authored-By.
Let hooks block ; report verbatim ; never pre-write antibody / ratchet
/ TESTS_SKIP / override markers — those are Chris's per-file calls.

STEP 0 — FRESHNESS CHECK (i643 ; this is not optional) :
This session proved 3x that a carded premise was DEAD because
origin/main moved (i637 commit got published ; i638 target aggregate
was stripped ; the i638 Rust mechanism was the wrong structure). For
EVERY SHA this card names : git merge-base --is-ancestor <sha>
origin/main — if YES, any rewrite/split instruction is a force-push to
main : STOP, treat obsolete. For every "the EXISTING X" : grep
origin/main ; if absent, the mechanism is dead : STOP, surface to
Chris. THEN execute. (i643 proposes mechanising this as
`storehouse restart-card verify` — worth doing early.)

BRANCH STATE (verify, do not trust) : branch i638-unit-e-cadence-vos,
PR #674, off origin/main 1b08a2d4. HEAD 7d22108e. Six commits :
ebffc7c2 behaviors baseline-red fix ; 835d0eb7 worktree-aware
pre-commit harness (antibody-exempt) ; 9ae35ca0 i637 closed + i642 ;
8e020295 i638 E-0 + i643 ; bae9e137 i638 E-1 (bluebook cadence VOs +
RunCadence) ; 7d22108e i638 revised + i644. loc-ratchet 0, no
override. behaviors 341/341. The +68 hand-written Rust VO mirror
(old a7abf271) was RESET OUT — do not resurrect it ; i630 stays
fixed+regression-locked on main via Unit B (98a8458c). If Chris has
merged #674, rebase/branch off the new main first.

THE MAIN WORK — i644 (finishes i638 bluebook-first) :
inbox/i644.md is the COMPLETE mechanical map. Proven this session :
VO `invariant` blocks are decorative framework-wide (parsed-and-
discarded, no evaluator, never enforced — inbox/pizzas/i638 all
inert). i644 builds the engine : (1) ir_shape fixtures add Invariant
type + ValueObject.invariants field ; (2) parser_shape captures the
invariant in parse_value_object ; (3) NEW rust/src/runtime/
invariant_eval.rs — bounded evaluator for the 3 DSL forms only
(.length>N / .match?(/re/) / .include?("s")) ; (4) runtime_shape
hooks it into VO coercion, recursing nested + list_of. Each step is
fragment-edit -> storehouse specialize <shape> -> byte-identity
golden. Acceptance probe : RunCadence with commands:[{verb:
"BadNoColons"}] must ERROR (today it PASSES). Then add the
ScheduledCommand `invariant value.include?("::")` and delete any
Rust FQN mirror : run_loop/run_clock become pure-bluebook shims
(parse argv -> attrs -> dispatch ; runtime validates). Commit per
step ; split by concern on ratchet calls (the new evaluator module
is the only ratchet-relevant .rs). i638 + i644 archive together.

THEN — Chris's queued directive : "do the same to mod.rs — break out
everything we can into bluebook". rust/src/runtime/mod.rs is huge and
is itself the runtime_shape byte-identity target. This is its own
multi-arc ; FILE IT AS A CARD with a scoped map before executing
(do NOT start it deep ; the i638 arc is the cautionary tale). It
pairs naturally with i644 step 4 (both touch runtime_shape).

DISCIPLINE NOTES from this session (read the i638.md retro) :
- The act of trying to execute is when premises break. Card big
  arcs BEFORE diving (i643). "keep going" through 3 premise-shifts
  into a kernel feature was the documented lost-hours pattern ;
  the clean outcome was the PR + the mechanical i644 map, not a
  half-build.
- When structure is wrong, rip it out fully (a7abf271 reset out) —
  no override, no dead abstraction left to "finish later".
- Bus discipline : I lapsed to native shell + Write twice ;
  Chris caught both. Route through the door.
```

## Closes when
The next session has run the freshness-check, knows where #674 stands,
and either lands i644 or re-cards it cleanly. Then archive.
