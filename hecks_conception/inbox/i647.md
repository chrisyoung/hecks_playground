---
ref: i647
status: open
priority: low
posted_at: 2026-05-19
posted_by: Miette (Chris: #20 card for mood)
category: statusline / mood
value: 'Mood is parked, not deleted. The awake statusline render (run_statusline/mod.rs render_awake) no longer surfaces the mood glyph/vibe ; heartbeat now leads. State.mood_glyph / mood_vibe and the SetMood command stay live and are still populated from heki records (state.rs) so nothing is lost. When Chris grows mood into something substantial, the render re-adds a segment ; until then it stays off the line to keep the statusline to its two essential signals (heartbeat + multi-inbox).'
---

# i647 - Mood parked off the statusline (returns when grown)

## Decision (Chris, 2026-05-19, task #20)

heartbeat to front ; remove mood from the render. Done in
run_statusline/mod.rs : the mood-prepend block is removed, heart leads.

## Not lost

- State.mood_glyph / mood_vibe : still fields, still set from heki in state.rs.
- MietteBody mood SetMood command : untouched, still dispatchable.
- Only the *render* dropped it. Re-surfacing later is a one-block change.

## Closes when

Chris decides what mood should become and the render gets a designed
mood segment back (or this is consciously closed as won't-do).
