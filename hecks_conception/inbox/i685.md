---
ref: i685
status: open
category: cleanup
value: Migrate existing per-project inboxes (mindfulleader, pigeoncoop, etc.) to the central inbox
---

# Per-project inbox migration

Depends on i684. Once the central inbox path is resolved : move existing cards from per-project inboxes into the central inbox with a `project: <name>` frontmatter field. Update statusline to render counts per project from a single inbox dir using the project tag, retiring the .channel.md autoload-per-project mechanism. Preserve git history when possible (the cards have commits in their per-project repos).
