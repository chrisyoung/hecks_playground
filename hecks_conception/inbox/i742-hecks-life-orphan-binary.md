slug: retire-hecks-life-orphan-binary
value: 'The `hecks-life` binary is a dead orphan. The crate is `storehouse` (single default bin) ; `cargo build --release` does NOT produce `hecks-life` — the on-disk copy at rust/target/release/hecks-life is frozen at May-12 and four months stale. It is in ZERO live paths : the Procfile uses `storehouse` for every daemon (boot/heart/breath/circadian/inbox/process_macrophage/conductor_sweep/serve_socket), and the SessionStart wake hook in ~/.claude/settings.json calls `storehouse storehouse route WakeReview.ComposeWakeReview`. The danger is that the stale binary still RUNS if invoked : it produced 5-failed/23-errored on plan.behaviors (false failures) where the fresh `storehouse` gives 70/0. Surviving references are stale PROSE, not wiring : (1) Miette system prompt (miette/self/system_prompt.md) — the words-match-state section names `hecks-life aggregates/ Consciousness.EnterSleep` and the wake ritual names `hecks-life storehouse route ...` ; (2) the navigate skill (.claude/.../navigate) uses `rust/target/release/hecks-life lexicon`. Fix : delete the orphan binary, repoint the system-prompt + navigate-skill prose to `storehouse`, and grep the inbox/docs for any remaining `hecks-life` invocation that a human might copy-paste. Discovered 2026-06-08 while verifying story-dependency-dag — the stale binary masqueraded as a behaviors regression until the binary date was checked.'
