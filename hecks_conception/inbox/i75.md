---
ref: i75
status: queued
priority: medium
value: 'all-dream-shells-to-bluebook-and-hecksagon'
posted_at: 2026-04-25
source: heki-migration
---

# i75 — all-dream-shells-to-bluebook-and-hecksagon

all-dream-shells-to-bluebook-and-hecksagon — Sweep follow-up to dream_review.bluebook (PR #440). Convert the remaining dream-related shell scripts to bluebook + hecksagon : rem_branch.sh (REM dream production — seed_dreams + dream_image_from_claude + lucid_observation), nrem_branch.sh (NREM consolidation narration), interpret_dream.sh (wake-time dream interpretation), capabilities/wake_report/wake_report.sh, mindstream.sh's wake hook block, sleep_cycle.sh. Each becomes an aggregates/<name>.bluebook + aggregates/<name>.hecksagon. The shell scripts stay as transitional adapters until the runtime can dispatch the declared commands end-to-end (same Phase F pattern). Note: rem_dream.bluebook already exists in capabilities/rem_dream/ — that's the dream-content-production bluebook ; this work extends the pattern to the wake/interpret/seed/consolidate side. Origin: Chris's 2026-04-25 correction 'and all the dream stuff needs to become bluebook — including the shell — use hecksagon.' Each conversion is small (one script → one .bluebook + one .hecksagon, ~60-150 lines each). Order of operations: do the smallest first (interpret_dream.sh, sleep_cycle.sh) to establish the pattern, then the larger ones (rem_branch.sh ~270 lines is the big one). After all six are bluebookified, file an inbox item to ACTUALLY DELETE the shell scripts once the runtime can dispatch their declared commands (Phase E pattern — declare, then the imperative host retires).
