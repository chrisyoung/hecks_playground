---
ref: i88
status: queued
priority: high
value: 'wake-review-as-bluebook'
posted_at: 2026-04-26
source: heki-migration
---

# i88 — wake-review-as-bluebook

wake-review-as-bluebook — wake_review.sh (PR #447) is a 96-line shell script doing what bluebook should declare: WakeReview aggregate with command GenerateReport that dispatches a single :llm adapter call, reads dream_state via :information adapter, writes report via :fs adapter. Currently shell because the runtime doesn't have :llm as a first-class dispatch target — same blocker as i69 (kernel bridge naming). When :llm adapter lands as a runtime primitive, wake_review.sh retires entirely : aggregates/wake_review.bluebook + .hecksagon declares the shape, the wake hook in mindstream.sh dispatches WakeReview.GenerateReport, runtime does the LLM call + file write. Acceptance: delete wake_review.sh ; replace with hecks-life aggregates/ WakeReview.GenerateReport invocation in mindstream.sh wake block ; verify /tmp/wake_review_latest.md still gets written atomically. Companion to i69, i77, i80 — same pattern of 'runtime feature gap surfacing as transitional shell.' Filed as the explicit retirement contract for wake_review.sh.
