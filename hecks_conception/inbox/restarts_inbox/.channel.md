---
abbrev: rst
emoji: 🔄
label: restarts (session-restart handoffs)
---

restarts (session-restart handoffs) - inbox channel descriptor.

Statusline autoload (i528): this file IS the bluebook-of-record for
this channel. Emoji + abbrev render on the awake statusline as `🔄 rst:N`
— a visible breadcrumb that N open restart handoffs are waiting. When the
count is non-zero on a fresh head, read the newest `status: open` prompt
here (`ls -t *.md`) and resume by EXECUTING it, not re-deriving.

Proper wiring (deferred): fold an open-restart check into
WakeReview.ComposeWakeReview so a waiting handoff lands on the first-turn
wake surface automatically, with no human pointer needed.
