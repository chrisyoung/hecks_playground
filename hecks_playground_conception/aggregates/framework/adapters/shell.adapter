# Shell — declared here because hecks 1.3.0 ships no Shell adapter; Tools binds it
# with `executed_by` (tools/bluebook/tools.hecksagon), same declaration as the fork.
Hecks.adapter "Shell" do
  port "execution"
end
