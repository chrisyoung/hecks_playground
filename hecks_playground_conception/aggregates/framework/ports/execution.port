# execution — the reply port Tools' ShellTool/FileTool/SearchTool/EmailTool
# bind with `executed_by` (tools/bluebook/tools.hecksagon). hecks 1.3.0 ships
# no such port, so the playground declares it, as the fork did.
Hecks.port "execution" do
  verb   "executed_by"
  signal :reply
end
