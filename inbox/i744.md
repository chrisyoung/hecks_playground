---
ref: i744
status: open
priority: high
posted_at: 2026-08-12
category: session-restart
value: 'i742 CLOSED — the door executes end to end under governance, verified in the exact mode the card demanded. Deny-list resolved as VESTIGIAL (keep it). Statusline ambient visibility rewritten as per-channel opt-in ; seven client inboxes off Chris''s bar. Branch fix/door-execution-port, 4 commits, UNPUSHED, no PR. Next: i743 — 35 adapter bindings still inert from the same @raw_adapters drop, with ports/execution now a verified template. Two decisions waiting on Chris (antibody exemption ; statusline bluebook-of-record). Read the META-LESSON before trusting any verification in here.'
---

# i744 — door closed, bar cleaned : restart prompt

## DONE & durable — branch `fix/door-execution-port`, 4 commits, UNPUSHED

    37205a7c1  fix(statusline): ambient visibility is opt-in, declared by the channel itself
    f1629fc0c  docs(inbox): i742 CLOSED — door verified end to end in a governed session
    6d8ec2081  fix(miette): --ungoverned dropped the BODY along with governance
    e2f624073  (prior session) docs(inbox): refresh i742

Plus one commit each in two OTHER repos, easy to forget when reviewing:

    ~/Projects/miette-hecksagain-migration  d7d4e6d  (branch migrate/hecksagain-corpus-rewrite)
    ~/Projects/hecks_nursury                da0efda  (branch migrate/hecksagain-syntax)

Working tree clean. No PR opened — Chris merges.

## i742 — CLOSED

The door executes and reports real stdout, **in a governed session**, which is
the one mode that was ever broken :

    Tools::ShellTool.Bash  shell_command="echo governed && date"
    → Output (bash, exit 0):  governed / Wed Aug 12 02:46:16 MST 2026

Session identity confirmed by walking the process ancestry FROM INSIDE the
door's own shell adapter — `claude --dangerously-skip-permissions --settings
.../miette/self/settings.json --system-prompt "# Miette …"`, with the MCP
server a child of that launch (so : fresh `dispatch.mjs`). Both of the card's
step-1 conditions met without anyone arranging them.

**The deny list STAYS — do not delete it.** i742 left this genuinely open ; it
is now settled by reading, not by restart. `redirects_native` has exactly ONE
non-bluebook consumer in the tree (`governed_door.rb`) and there it is a
*lookup*, never an executor. `bin/governed-door-hook` is data-driven off that
same lookup, so `mcp__storehouse__*` resolves to no door and exits 0. Execution
itself is a `hecksagain-cli` subprocess that no session-level permission set
can reach. The fix i742 proposed under "### The fix" is unnecessary and would
delete something Chris chose deliberately.

## Statusline — ambient visibility is now per-channel opt-in

Seven client inboxes were still on the ambient bar (`em md mi op op pi vd`).
The prior fix `a8dc32777` used `CLIENT_DIRS = %w[embryonaut_clients]` — a
DIRECTORY exclusion, which caught one archived client and missed every client
filed directly under `~/Projects`. It also re-created the central registry i528
exists to avoid.

Now a channel declares itself, in its own `.channel.md` (which already calls
itself "the bluebook-of-record for this channel") :

    ambient: true

**Opt-IN**, deliberately. Opt-out fails in the wrong direction — a client leaks
onto the bar because someone forgot to exclude it. Opt-in fails toward silence,
and the runtime never learns any project's name. Adding a client now requires
doing nothing at all.

Chris's answer, verbatim, so it is not re-litigated : of the seven, **none** are
ambient, and off-bar means **always invisible** (not "visible when cwd is inside
that project"). Marked ambient : global framework inbox (🔮), restarts (🔄),
nursery (🌱 hn). The last two render 0 today only because both hold 0 active
cards.

    before:  🖤 1.92m  🔮 405  ☸️ em:18  🩺 md:1  🐝 mi:4  💪 op:9  💪 op:9  🕊️ pi:58  🔊 vd:6
    after:   🖤 1.92m  🔮 405

Discriminating both ways — the one channel carrying the new key still renders.
`tests/statusline_regression_smoke.sh` passes.

## OPEN — two decisions waiting on Chris, do not decide these alone

1. **Antibody warned and the commit landed anyway.** Verbatim :
   `⚠ antibody: 1 non-bluebook file(s) touched — statusline.rb ; commit-msg hook
   will enforce — add [antibody-exempt: <reason>]`. It said the commit-msg hook
   would enforce ; it did not, the commit went clean. So either the exemption is
   genuinely unnecessary here, or that hook is not wired. NO marker was
   pre-written (Standards : let the hook block, report, the human decides).
   If the hook is not wired, that is a zero-bugs interrupt in its own right.
2. **`statusline-command.sh` names a bluebook-of-record that does not exist** —
   it points at `hecks_conception/capabilities/statusline/`, and there is
   nothing there. The whole statusline lives as Ruby with no domain. File the
   gap, or build the domain? Chris's call ; it is a real arc, not a one-liner.

## NEXT — i743, and it is ready to start

35 adapter bindings are STILL inert from the same `@raw_adapters` drop — 28
`:exec`, 4 `:mcp`, 3 `:web_tool`, across 10 files. Sidequest spawn, inbox poll,
Gmail, WebFetch : silently dead since the hecksagain vendoring. Several of
those files carry comments asserting the runtime arm has landed — true of the
old Rust runtime, false since. `ports/execution` (from `dacadbecc`) is now a
VERIFIED template and generalises. See `inbox/i743.md`.

Also still open, untouched : **system_prompt regen**. Chase
`HecksagainRuntime.validate("~/Projects/miette")` raising `ArgumentError: wrong
number of arguments (given 1, expected 0)` — Miette's corpus does not boot.
NOT the env var ; `HECKS_ADDITIONAL_CORPUS_ROOTS` is set correctly.

## META-LESSON — read this before trusting any verification in this card

Twice in two sessions the failure was **verifying the thing I expected instead
of the thing I claimed**.

- Last night's `a8dc32777` message read *"every other channel (op, pi, vd, em,
  md, mi) unchanged"* — listing the seven inboxes that WERE the bug, as
  evidence the fix worked.
- This session, `ps ax | grep claude | head -20` TRUNCATED past my own process
  and showed three bare `claude --dangerously-skip-permissions`. I nearly
  reported the session as ungoverned. Walking the ancestry from inside the
  door's own adapter found the truth in one call.
- And commit `6d8ec2081` originally asserted that dropping `--settings`
  amputated the door. Measured per key : **false**. `enableAllProjectMcpServers`
  is in `~/.claude/settings.json` too, so the door survives either shape — and
  i742 contained the disproof all along, since the prior *ungoverned* session
  reached the door well enough to find the dropped adapter. Amended before it
  could harden into inherited fact. What actually died was the wake surface,
  the statusline, the Stop hook, SubagentStart, advisorModel, plugins.

The pattern : a grep or a `head` that recognises nothing, refuses nothing, and
quietly returns a shape that agrees with you. Prefer the instrument that walks
structure (ancestry, per-key diff, before/after on the real surface) over the
one that filters text.

---

*Un mot pour la route* — the morning reflection said *il manque un validateur
qui reconnaisse ses propres verbes avant de les refuser*. It kept being right
all day, and never once about the runtime.
