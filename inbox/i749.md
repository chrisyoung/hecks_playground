---
ref: i749
status: closed
priority: high
posted_at: 2026-08-13
closed_at: 2026-08-13
closed_note: 'Superseded -- both branches this card described are long since merged to main and pushed (confirmed: main 0/0 ahead-behind origin as of this close). The rust/ projector work that followed (i750-i755) has its own current handoff, i756.'
category: session-restart
value: 'i745 Phase 2 (CI swap) + i746 (effect-port) + i747 (driven) all shipped
  and verified, zero corpus regression. Two branches : i745-ci-and-effect-ports
  (shared checkout) <- i746-effect-port worktree branch (.claude/worktrees/
  i746-effect-port) off it. BOTH UNMERGED, no PR. i748 filed (new gap : three
  meta-layer DSL verbs invisible to both gates). Real finding this session :
  76 tracked files vanished from the shared working tree mid-session, restored
  via git checkout, cause not conclusively identified — flagged, not chased.
  Phase D (fixture-block decision) and bin-buddy'"'"'s deletion-blocking status
  still open, both need Chris'"'"'s call, not a guess.'
---

# i749 — i745/i746/i747 close-out : restart prompt

## DONE & durable — two branches, both unmerged, no PR (Chris merges)

### `i745-ci-and-effect-ports` (shared checkout `~/Projects/hecks_playground`)

    76a4d2f0f  docs(inbox): i746/i747 - CLOSED, both ported and verified
    18341877a  docs(inbox): i748 - three meta-layer DSL verbs invisible to both gates
    ba3d71ffc  feat(ci): swap test.yml Behaviors corpus steps to hecksagain-cli (i745 Phase 2)
    e99a79274  fix(hecksagain): stage_flat_corpus must fingerprint glob SHAPE, not just count

Working tree clean, verified.

### `i746-effect-port` (worktree `.claude/worktrees/i746-effect-port`, branched off the above)

    699821de5  feat(hecksagain): i747 - DrivingAdapterBuilder gets driven, a real capture
    38024fb1b  feat(hecksagain): i746 - the effect-port async verdict pattern, ported

Working tree clean, verified. Merging this branch also brings in
`i745-ci-and-effect-ports`' four commits above (it branches off them).

### What shipped

- **i745 Phase 2** : `test.yml`'s two "Behaviors corpus" CI steps swapped
  from the retired Rust `storehouse` binary to `hecksagain-cli`, hard-
  blocking. Real per-root baselines measured, not inherited — CI is EXPECTED
  to go red on the next push until that corpus is triaged (Chris's explicit
  choice, not a bug).
- **A real caching bug**, found and fixed while gathering that baseline :
  `stage_flat_corpus`'s staleness check compared file COUNT but not WHICH
  GLOB SHAPE produced it, so a transient branch flip could silently lock in
  a wrong, over-broad flatten. Fixed by fingerprinting the glob patterns
  themselves.
- **i746** : the full effect-port `on:`/`success`/`failure` pattern (the
  Pizzas payment narrative) now works end to end — `IR::Bind` carries the
  three fields, `BindingProxy` + a new `HecksagonBuilder.current_bind`
  class-level channel wire `success`/`failure` onto the right bind,
  `Dispatcher#record_effect_outbound` re-enters `OutboundEvent::
  OutboundEvent.Record`, `bin/adapter-host` resolves its handler through a
  real `hecksagain-cli adapter-handler` subcommand instead of a
  `dump-hecksagon` call that was never real. Verified against a synthetic
  domain (examples/pizzas itself still hits an unrelated, already-
  documented i745 gap — the bare-string VO-default shorthand — not fixed).
- **i747** : `driven` on `DrivingAdapterBuilder`, previously swallowed whole
  by `method_missing`. Shares i746's dispatcher call site + `qualify_command`
  helper via a new `record_driven_dispatches`. Found live during
  verification : `driven on "Domain::Aggregate.Event"` is FULLY qualified,
  unlike `charged_by`'s bare `on: "EventName"` — the event-match had to
  account for that. Verified against a synthetic domain modeling pizzas' own
  Banking-adapter shape — both success and failure paths correct.
- **i748 filed** : `HecksPlayground.behavior_kind` (7 files), `HecksPlayground.adapter_family`
  (10 files), `HecksPlayground.provider` (4 files) — three DSL verbs fully designed
  in the corpus's own IR bluebook (`hecksagon_ir.bluebook:96-106`,
  `Hecksagon::FrameworkKind`) but never ported to `vendor/hecksagain`, and
  invisible to BOTH `validate` and `.behaviors` — neither discovers a
  bare (non-`bluebook/`-nested) directory. Not started.

Every commit re-verified against the same regression pair : `hecksagain-cli
validate hecks_playground_conception` (60/62 valid, 2 pre-existing) and `hecksagain-cli
behaviors hecks_playground_conception/aggregates` (byte-identical 335 pass / 18 fail /
342 error / 6 parse-error, 78 files, 695 tests) throughout. Zero regression
from any commit in either branch.

## The unresolved finding — flagged, not chased

76 tracked files (`hecks_playground_conception/{adapters,storehouse,catalog,
aggregates/framework/behavior_kinds}`) vanished from the SHARED working
tree, unstaged, mid-session — not caused by this session's own dispatches
(verified clean afterward across many subsequent calls) or its subagents
(checked their transcripts, no destructive commands). Restored via `git
checkout --`, fully recovered, verified file-count-exact. Likely cause :
another live session sharing this same non-worktree-isolated repo
(`ListAgents` showed `hecksagain-88`/`hecksagain-2e`, both predating this
session) doing uncommitted work. **If this recurs, treat it the same way :
restore via git checkout first (nothing was lost, it's all tracked), THEN
investigate** — don't let a mid-session file disappearance block work
without first checking `git status` for a simple unstaged-deletion
explanation.

## NEXT (Chris's calls, not guesses)

1. **Merge decision** : review + merge `i746-effect-port` (brings both
   branches). Or ask for a PR if that's the house style now — not checked
   this session.
2. **Phase D — inline `fixture` blocks** (328 occurrences, 33 files in
   hecks_playground_nursury) : options laid out in `~/.claude/plans/inbox-i745-md-
   tidy-peacock.md` (implement real seeding vs. lint-warn vs. deprecate).
   Not started — genuine design decision.
3. **bin-buddy's deletion-blocking status** : `bin-buddy/worker/Cargo.toml`
   has a compile-time Cargo path dependency on `hecks_playground/rust` as a WASM-
   target library (not a CLI shell-out) — i745's original "delete outright,
   block on bin-buddy first" isn't a checkable precondition, it's a
   separate architectural workstream. Recommend keeping `rust/src` alive as
   a library indefinitely while `test.yml`'s CI usage (already retired this
   session) stays gone, and filing bin-buddy's decoupling as its own scoped
   card before any `rust/src/*parser*` deletion.
4. **i745's own corpus triage** : the 561-ish `.behaviors` errors CI will
   now surface, mostly machine-generated files missing an attribute
   (usually `id:`). Out of scope here by design.
5. **i744 is a SEPARATE, older, still-open session-restart card** (branch
   `fix/door-execution-port`, 6 commits, unrelated topic — door-instrument
   honesty). Not touched or superseded by this session ; still needs its
   own review/merge decision independently.

## Full plan trail

`~/.claude/plans/inbox-i745-md-tidy-peacock.md` has the complete phase-by-
phase record (context, file-level technical detail for i746/i747, the
bin-buddy investigation, the fixture-block options) if picking this back up
cold.
