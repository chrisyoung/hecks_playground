ref: i767
status: open
priority: low
posted_at: 2026-08-14
category: session-restart
value: 'Session closed clean, nothing half-finished. bin-buddy''s
  hecksagain-WASM cutover (i766) went all the way to production this
  session : PR #19 merged, worker + Pages both deployed, verified with a
  real R2 round-trip (write via authenticated POST, read back with
  wrangler r2 object get --remote, byte-identical). Two real deploy-time
  bugs found and fixed along the way, not left as "should work" : a
  Worker/Pages secret drift, and a Pages redeploy that silently dropped
  its own Functions bundle. hecks main : clean, 0/0 ahead-behind, pushed.
  bin-buddy : clean, main, nothing pending. Open threads are all filed,
  none blocking : i762 (cross-aggregate expression gap), i763 (summer''s
  own parser migration), i764 (undeclared-attribute-type DSL validation
  gap), i765 (generated/<chapter> same-name collision).'
---

# i767 -- session close-out : i766's production deploy landed clean, nothing open blocks a restart

## DONE & durable, this stretch

See **i766** (closed 2026-08-14) for the full deploy sequence and its two
real caught-and-fixed bugs -- not duplicated here. Short version: bin-buddy
now runs its Worker + Pages proxy fully on hecksagain's own wasm-bindgen
projection in production, verified with an actual R2 write-then-read
round-trip through the live stack, not just a green status code.

hecks main: `git status --short` empty, `origin/main...main` is `0 0`,
pre-push integrity + `.behaviors` gates green on every push. bin-buddy:
`git status --short` empty, on `main`, PR #19 merged and nothing else
pending.

## Open, not blockers, filed and left for whenever picked back up

- **i762** -- cross-aggregate expression grammar gap in bin-buddy's own
  bluebook (query calls, `Date.today`, `%w[]`, multiplication, live
  cross-aggregate field dereference). Confirmed a genuine gap in both
  Ruby's and Rust's canonical evaluator, not a parity issue. Real design
  work, out of scope for the plan that just closed.
- **i763** -- `summer`'s own migration off `hecks/rust/src`. Three real
  options laid out, none picked -- Spring's call.
- **i750** (reopened this session, still open) -- the VO-namespacing
  design fix itself (4 of hecks_conception's own `plan` domain's 5
  remaining compile errors). Chris's own "qualify by owning aggregate"
  answer from the bin-buddy plan-mode conversation is the chosen
  direction when it's picked up -- deliberately not implemented this
  session, real generator-wide work.
- **i764** -- `Registry#verify!` lets an attribute reference an
  undeclared value_object type through silently instead of refusing it.
- **i765** -- `rust/src/generated/<chapter>` is keyed by bare chapter
  name only ; two unrelated same-named domains clobber each other
  locally. Candidate fixes listed, none chosen.

None of the above block a restart -- they're real, scoped, and waiting,
not urgent or half-done.
