ref: i768
status: closed
priority: high
posted_at: 2026-08-14
category: session-restart
value: 'Waves 2-5 done + all 6 originally-open follow-up tasks resolved
  (7, 8, 17, 20, 21, 22). Two of the fixes found REAL, striking cross-
  runtime bugs: identified_by support gaps in BOTH Ruby (bare-symbol,
  fixed) and Rust (brace form, 273 sites, still open) parsers, found by
  two agents blind to each other. 3 PRs open against hecks_playground (#761/762/763),
  none merged -- awaiting Chris review. 4 real design decisions filed
  for Chris (17, 26, 27, 28), none mine to make. One self-inflicted
  branch-basing mistake happened and was fully recovered (PR #762 briefly
  would have reverted Wave 5.1 if merged -- caught before any real
  damage, rebuilt clean). Governance hooks still point at hecks_playground_conception,
  not miette -- deliberately, corpus still cant fully boot (task #26 is
  the direct blocker).'
---

# i768 -- Wave 2-5 + all originally-open tasks resolved; 3 PRs awaiting review, 4 decisions awaiting Chris

## Where things stand, top to bottom

**Waves 2-5**: done (see this card's own prior revision in git history for
the full account, or `~/.claude/plans/hecks-hecksagain-cutover-prd.md`'s
Execution Slices section). `hecks-hecksagain` main now at commit `a984054`.

**All 6 originally-open follow-up tasks resolved this pass**:
- **#7** (driving-adapter staging fix) -- verified working in isolation,
  but introduces a regression when merged onto the real fork tip (see #25
  below, in progress).
- **#8** (TaskTool.Get missing args) -- fixed in 4 hecksagons (found a
  4th file, `interval_adapter`, beyond the original 3), plus a SECOND
  bug in the same files (stale `HecksPlayground::Framework::Tools::` FQN prefix
  instead of the domain's real name `Tools::`) that would've kept them
  broken even with the args fix alone. PR #762.
- **#17** (pigeoncoop reconciliation) -- NOT resolved, correctly left for
  Chris. Two diverged domain designs, real business decision.
- **#20** (VO-typed `default:` grammar drift) -- confirmed intentional,
  tested fork grammar (not a bug); fixed as content in both `hecks_playground` and
  `miette`. Found a second bug in the same sweep (`then_delete` doesn't
  exist anywhere in hecksagain's grammar) -- filed as #26.
- **#21** (48->53 test regression) -- the "53" was itself shell
  contamination (`HECKS_PLAYGROUND_GOVERNANCE_OFF=1` ambient), real baseline was 40;
  now 0 failed/40 ignored/1070 passed, every ignore precisely reasoned.
  Found a striking parallel to #22 (see below). Filed #29, #30.
- **#22** (governance corpus-load bug) -- root cause wasn't what I
  guessed (unknown DSL keywords) -- it was `identified_by`'s bare-symbol
  form wrongly deleted from hecksagain's grammar on a false "zero corpus
  usage" premise (108 real lines use it). Recovered + cherry-picked a
  real, already-written fix from the fork's own `split/*` branches.
  Fixed 10 more miette content files along the way. Governance hooks
  still NOT repointed at miette -- 3 real blockers remain, all filed as
  Chris decisions (#26, #27, #28).

**The striking cross-runtime finding**: #21 and #22, working blind to
each other, both found `identified_by` form-support gaps -- #22 in
Ruby (bare-symbol form wrongly deleted, now fixed), #21 in Rust (brace
form `{ field.value }` silently unparsed at 273 real corpus sites,
NOT yet fixed -- filed as #29). Same defect class, opposite runtimes.
Worth a real look together given this repo's whole Ruby/Rust parity
model.

## PRs against `hecks_playground` -- ALL MERGED to `main`

- **#761** -- MERGED. The big Wave 2-5 migration (Gemfile cutover,
  `rust/` retirement, DEAD-directory deletions). Final ref: `48d2210`
  (picks up tasks #20/#21/#22/#25's fork fixes).
- **#762** -- MERGED. TaskTool.Get args + gemspec/HecksPlayground-constant
  collision fix (task #8/#24). Had a real conflict with #761's branch
  (task #25 independently re-fixed the same 4 hecksagons, keeping the
  OLD verbose `HecksPlayground::Framework::Tools::` FQN prefix rather than the
  corrected short `Tools::` form) -- resolved in favor of the corrected
  short form, since it's the fundamentally right fix regardless of
  task #25's separate runtime tolerance improvements.
- **#763** -- MERGED. VO-typed default + stale `identified_by` syntax
  fix (task #20). Clean merge, no conflicts.

`main` verified directly post-merge: clean `bundle install`, real
`Tools::TaskTool.Get` dispatch succeeds with zero warnings (the
`already initialized constant HecksPlayground` warning is gone too -- task #24's
gemspec fix landed clean).

## A real mistake this pass, fully recovered

Branched a fix (#762's precursor) from a STALE point (before Wave 5.1's
deletions landed) instead of the actual current PR #761 tip. Merging
task 7's properly-based branch into it surfaced the divergence --
would have resurrected `adapters/`, `docs/`, `rust/` (reverting Wave
5.1) if merged as-is. Caught via direct verification before pushing
further, root-caused, rebuilt both fixes cleanly on the real tip in an
isolated worktree, force-pushed the correction. PR #762 now correctly
shows a 5-file, +30/-12 diff. No data was lost at any point (git's
object database kept everything reachable); this was a process mistake,
caught and fixed, not a data-loss incident.

## Open, needing Chris's decision -- none mine to make

- **#17** -- pigeoncoop: which of two diverged domain designs wins.
- **#26** -- `then_delete` doesn't exist in hecksagain's grammar
  (blocks `antibody.bluebook`'s `ExemptRegistry.RetireExemption`, and
  is THE direct blocker to finally repointing the governance hooks at
  `miette`). Admit a delete verb, or remodel as a lifecycle state.
- **#27** -- 3 imperative-Proc process-manager handlers (`mind.bluebook`,
  `lucidity.bluebook`x2) now hard-crash at LOAD time, not just dispatch.
  Which Phase 2.c path: conditional dispatch, multi-source ValueSpec,
  or template ValueSpec.
- **#28** -- `body/cycles` (heart_tick/breath_tick/body_tick) need
  multi-dispatch `driving on interval` handlers; the fork's grammar
  only supports one dispatch per handler. Admit multi-dispatch, or
  split each into two adapters.

## Open, real technical work, not decisions

- **#25** -- CLOSED. Root cause was 3 real bugs, not the suspected
  auto-merge issue: fork DSL missing a `run` leaf, fork runtime missing
  deep-namespace verb resolution on the driven side, two hecks_playground_conception
  content defects. All fixed, verified zero-regression across a 924-test
  corpus sweep + full fork rspec. Fork fixes on `hecks-hecksagain` main
  (`668c5d3`, `48d2210`); folded into PR #761 (`chore/hecksagain-gemfile-
  cutover` fast-forwarded to `67028eabd`, Gemfile ref now `48d2210`).
- **#29** -- Rust parser silently fails on `identified_by { field.value }`
  brace form, 273 real corpus sites. Deliberately not fixed yet
  (identity-minting change at 273 sites is above one task's call).
- **#30** -- `hecks-hecksagain`'s storehouse test suite isn't genuinely
  CI-portable yet (9 files + 2 subtests depend on a hardcoded local
  path that happens to resolve correctly on this machine today).

## NEXT (picked back up whenever)

1. Wait for #25's resolution (in flight).
2. Review and merge #761, #762, #763 (or request changes).
3. Make the 4 decisions (#17, #26, #27, #28) whenever convenient --
   #26 specifically unblocks the governance-hook repoint, the last
   real piece of Wave 3's original intent.
4. #29 (cross-runtime identified_by parity) is worth a dedicated look
   given how it mirrors #22's fix -- possibly a shared root cause.
</content>
