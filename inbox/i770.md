ref: i770
status: open
priority: medium
posted_at: 2026-08-15
category: session-restart
value: 'Session opened by reading i769 (i768 close-out). Chris made all 4
  filed decisions (#17/#26/#27/#28) via AskUserQuestion -- NONE implemented
  yet, decisions only. Along the way found + fixed a real infra bug: this
  session had booted holding a stale worktree'\''s hook config
  (miette-hecksagain-migration), now removed. Session itself still shows
  the resulting cosmetic hook-error noise until restart (fail-open,
  functionally harmless, cannot be hot-fixed mid-session).'
---

# i770 -- session close-out : 4 decisions collected (not yet built), stale migration worktree removed

## Decisions made this session -- ready to implement, none coded yet

- **#17 (pigeoncoop)** -- Chris: **keep the repo's existing design.**
  `~/Projects/pigeoncoop`'s own `hecks_playground/pigeoncoop.bluebook` (live
  site+worker) wins. The sandbox-imported branch
  `import/sandbox-pigeoncoop-bluebooks` (conception/ dir, landed i768
  slice 4.1) stays unmerged, left in place as the safety net per that
  commit's own message -- do not delete it, do not touch the live
  site/worker.

- **#26 (`then_delete`)** -- Chris: **remodel as a lifecycle state, no
  new delete verb.** `antibody.bluebook`'s `ExemptRegistry.RetireExemption`
  should transition to a `retired` state rather than deleting the record.
  This is a content-only fix (no hecksagain fork grammar change needed) --
  unblocked, doable now. **Before calling it done**: audit every existing
  query over `ExemptRegistry` for a state guard -- if none exists, a
  "retired" exemption will still read as active and governance will wrongly
  keep honoring it. This is THE direct blocker to repointing the governance
  hooks at miette (see i768) -- once fixed, that repoint is unblocked too.

- **#27 (3 imperative-Proc handlers, hard-crash at LOAD)** -- Chris:
  **mixed, case by case.** Three sites, in
  `~/Projects/miette/mind/bluebook/mind.bluebook` and
  `~/Projects/miette/body/dream/bluebook/lucidity.bluebook`:
  1. `mind.bluebook` `PhaseElapsed` (engaged_in_lucid_dream tick) --
     string-interpolates `toward: "I'd like to go deeper into #{target}
     with this"`. **Decision: grammar extension** -- lands a `template:`
     ValueSpec form on Surface B.
  2. `lucidity.bluebook` `LucidRemEntered` (dormant->rejoined bridge) --
     needs conditional/early-return dispatch, skip `unless mind_present`.
     **Decision: grammar extension** -- lands `when:`/`unless:` conditional
     dispatch clauses on Surface B.
  3. `lucidity.bluebook` `PhaseElapsed` (steering tick) -- needs
     multi-source OR-fallback
     (`pm.attributes[:steering_target] || event.attributes[:target]`),
     skip if both absent. **Decision: push to the receiving command** --
     `LucidDream.SteerDream`'s own given-clause absorbs the fallback,
     matching the SleepCycle lucid-vs-regular routing precedent the code
     already calls "preferred" in its own DEFERRED comment.
  All 3 need hecksagain fork grammar work for cases 1+2 (Surface B
  extension) before the bluebook content can be rewritten declaratively.

- **#28 (`body/cycles` multi-dispatch)** -- Chris: **admit multi-dispatch
  grammar.** The fork's `driving on interval` handlers gain support for
  more than one dispatch per handler, rather than splitting
  heart_tick/breath_tick/body_tick into paired single-dispatch adapters.
  Fork grammar work, same as #27 cases 1+2.

**Net: #26 is the one clean, unblocked, content-only arc -- do that first.
#27 (cases 1+2) and #28 both need the same underlying hecksagain fork
checkout** (`bundle info hecksagain` -> pinned `48d2210`, actual path
`~/.rbenv/versions/3.3.7/lib/ruby/gems/3.3.0/bundler/gems/hecks-hecksagain-48d2210b35ff`
-- a bundler gem cache, not a working git checkout with a remote to PR
against; find or create a real working checkout before starting fork
grammar work).

## Infra bug found + fixed this session

This session's `PreToolUse`/`PostToolUse` hooks (`governed-door-hook`,
`macrophage-hook`) were firing non-blocking errors on every tool call,
looking for scripts at `~/Projects/hecks_playground/bin/governed-door-hook` and
`.../macrophage-hook` -- **which never existed at that path**, no git
history there ever. Root cause: this session had booted holding the hook
config from `~/Projects/miette-hecksagain-migration/self/settings.json`,
a **leftover git worktree** from the merged-and-closed i768 migration.
The real, current, correct hook scripts live at
`~/Projects/miette/tools/bin/{governed-door-hook,macrophage-hook}`
(both present, both dated 2026-08-14 19:09, from the same i768 work) --
`~/Projects/miette/self/settings.json` already points there correctly.

Verified the stale worktree's branch (`migrate/hecksagain-corpus-rewrite`
@ `d7d4e6d`) was NOT an ancestor of miette `main` -- diverged, not merged
-- but its working tree had zero uncommitted changes and its content
appeared independently superseded by main's own later commits (matching
commit messages/scope, e.g. "i768 task 22"). Confirmed with Chris before
deleting (initial claim of "merged and closed" was wrong and I said so).
Removed: `git worktree remove` + `git branch -D` on
`~/Projects/miette` for both the worktree and the dead branch.

**This session will keep showing the cosmetic hook-error noise until it
ends** -- hook config loads once at session start and isn't live-reloaded.
Fail-open by documented design (`governed-door-hook`'s own policy: "a
wedged governance machine must never brick the session"), so nothing was
actually ungoverned beyond what the fail-open policy already accepts.
Next session boots clean.

Separately, smaller and NOT fixed (low priority, noted only): the
SessionStart hook in `~/Projects/hecks_playground/.claude/settings.local.json` points
`storehouse run` at `runtime/boot/bluebook/boot.bluebook`, a path with no
git history in `hecks_playground_conception` -- ever. The real file is at
`catalog/bluebook/boot.bluebook` (exists in both `hecks_playground_conception` and
`miette`), but even that lacks a declared `entrypoint`, so `storehouse
run` can't execute it directly either way. Also fails non-blocking, also
cosmetic. Someone should eventually either fix the path + add an
entrypoint, or drop the hook if `storehouse run` was never really the
right shape for whatever this was meant to boot.

## Open, filed, none blocking a restart (carried from i769, unchanged)

- **#29** -- Rust parser silently fails on `identified_by { field.value }`
  brace form, 273 real corpus sites. Mirrors #22's Ruby-side fix.
- **#30** -- `hecks-hecksagain`'s storehouse test suite isn't genuinely
  CI-portable yet (depends on a hardcoded local path).

## Next session should

1. Implement **#26** first (content-only, unblocked, highest payoff --
   unblocks the governance-hook repoint too). Audit `ExemptRegistry`
   queries for the state guard before calling it done.
2. Find/establish a real working git checkout of the hecksagain fork
   (not the bundler gem cache) before starting #27 (cases 1+2) or #28 --
   both need Surface B grammar changes there.
3. #17 needs no code -- just leave `import/sandbox-pigeoncoop-bluebooks`
   unmerged, as-is.
