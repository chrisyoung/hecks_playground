---
ref: i771
status: open
priority: medium
posted_at: 2026-08-15
category: session-restart
value: 'Closed the i770 boot.bluebook cosmetic gap for real -- root cause was
  a dead duplicate SessionStart hook, now removed. Along the way found a
  BIGGER live issue i770 underestimated: the real wake pipeline
  (~/Projects/miette/self/settings.json) depends on the same orphaned,
  git-deleted rust/target/release/storehouse binary the dead hook did --
  not touched yet, needs a decision.'
---

# i771 — boot.bluebook hook: root-caused + removed ; bigger binary-path rot found, not yet fixed

## Session flow

Woke clean (i770's stale-worktree fix held — verified `git worktree list`
on `~/Projects/miette` shows only the real tree, `migrate/hecksagain-corpus-rewrite`
branch gone, hooks correctly pointed at `~/Projects/miette/tools/bin/*`).
Relayed i770's 4 collected decisions (#17/#26/#27/#28, none built) to Chris.
He picked: fix the `boot.bluebook` SessionStart hook first (the low-priority
item i770 filed but didn't chase).

## DONE — i770's boot.bluebook item, actually closed

**Root cause, verified against the real source** (not assumed): the hook in
`~/Projects/hecks/.claude/settings.local.json` (gitignored, local-only, no
git history) called `storehouse run runtime/boot/bluebook/boot.bluebook` --
a path with no git history anywhere. Confirmed via git log that
`hecks/rust/` (and its `runtime/` sibling concept) doesn't exist as a
tracked path at all.

Checked whether pointing it at the real file (`catalog/bluebook/boot.bluebook`)
would even work: read `~/Projects/hecks-hecksagain/storehouse/src/run.rs`
directly (the actual current storehouse CLI crate -- **not**
`~/Projects/hecks/vendor/hecksagain/rust`, a different crate, name="rust",
unrelated to the `storehouse` binary; caught this wrong-tree grep via
advisor before concluding anything). `entrypoint` **is** fully wired --
`storehouse run` dispatches whatever `entrypoint "Aggregate.Command"` a
bluebook declares at its top level. `catalog/bluebook/boot.bluebook`
declares none, so even a path fix would still fail at that check.

Then checked whether this hook was even the thing producing the real wake
surface. It isn't: `~/Projects/miette/self/settings.json`'s own
SessionStart hook fires `storehouse route WakeReview.ComposeWakeReview`
-- that's the actual pipeline behind the ASCII wake card + dream/no-dream
line every session. `boot.bluebook`'s content (a `Session`/`BrainProbe`
aggregate modeling a Claude-vs-Ollama brain probe) reads like a pre-overmind
boot concept nothing downstream still consumes -- no policy, no query, no
other bluebook references it.

**Fix applied**: removed the dead `hooks.SessionStart` block from
`~/Projects/hecks/.claude/settings.local.json` (repair-forward would have
meant reviving a boot mechanism the real infra already superseded --
wrong call). File is gitignored/local-only so this is a clean, low-risk
edit, not a shared-infra change. Left the `permissions.allow` block intact.

## FOUND, NOT FIXED — the bigger thing under the cosmetic bug

Both the now-removed dead hook *and* the real wake pipeline
(`~/Projects/miette/self/settings.json`) pointed at the **same binary**:
`/Users/christopheryoung/Projects/hecks/rust/target/release/storehouse`.

Git proves that path is dead: `20725e683 chore(hecksagain-cutover): delete
hecks/rust/ in full, including target/ (i768 slice 2.2c)`. The binary
answering calls today only exists because a gitignored `target/` dir
survived that `git rm -rf` on disk and got rebuilt as recently as
2026-08-14 17:38 by something. `~/.local/bin/storehouse` (what's on
`$PATH`, what most tooling calls bare `storehouse` through) is a **symlink
into that same orphaned tree**.

The real current source lives at `~/Projects/hecks-hecksagain/storehouse/`
-- plus three parallel worktree slices (`hecks-hecksagain-slice-1.2`,
`-1.4`, `-1.6`) and a `hecks-hecksagain-migration` worktree not yet
inspected. Haven't determined which of these is canonical, or whether the
orphaned `hecks/rust/target/` binary is even behaviorally current vs. the
real crate (it was rebuilt today by *something* -- unclear what, or from
where, since the source dir it would need doesn't exist).

**Did not touch this** -- per advisor's call, repointing means choosing
among five checkouts and rebuilding, and the path currently still resolves
(proved via a live `storehouse --help` call this session). This is
provenance rot with real blast radius (my own wake pipeline depends on it
surviving), not an active break. It's Chris's decision, not a silent fix.

## Next session should

1. **Decide the storehouse binary provenance question**: which of
   `~/Projects/hecks-hecksagain/storehouse/`,
   `~/Projects/hecks-hecksagain-slice-{1.2,1.4,1.6}/storehouse/`, or
   `~/Projects/hecks-hecksagain-migration/rust/` is the canonical current
   checkout. Then repoint `~/.local/bin/storehouse` (and
   `~/Projects/miette/self/settings.json`'s hardcoded absolute path) at a
   real build from it, and delete the orphaned `~/Projects/hecks/rust/`
   directory entirely (it's already fully deleted from git -- only the
   gitignored `target/` survives on disk).
2. Figure out **what rebuilt** `hecks/rust/target/release/storehouse` as
   recently as today 17:38, given its source directory doesn't exist. A
   build script or CI step may still be pointed at the dead path.
3. Then return to i770's still-open queue: **#26** (ExemptRegistry
   retire-as-lifecycle-state, unblocked, highest payoff) is still the
   recommended next content task; #27 (cases 1+2) and #28 both need a real
   working git checkout of the hecksagain fork before Surface B grammar
   work can start -- likely the *same* checkout this session's binary-
   provenance question needs to resolve anyway, so do that first.
4. #17 still needs no code -- leave `import/sandbox-pigeoncoop-bluebooks`
   unmerged, as-is.
