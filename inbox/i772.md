---
ref: i772
status: resolved
priority: high
posted_at: 2026-08-15
category: session-restart
value: 'RESOLVED. i771''s storehouse binary provenance question closed : installed
  a real (non-symlink) build to ~/.local/bin/storehouse, repointed all 14
  mindstream.bluebook command literals + Procfile (hand-patched, generator
  itself separately broken) + .mcp.json + miette settings.json. Found and
  fixed a bigger live problem along the way : overmind itself was fully dead,
  13/14 body processes (heart, breath, circadian, speech_stream, etc.) were
  not running, only an orphaned BodyPulse survived. Cleared a stale
  .overmind.sock, restarted -- all 12 persistent members running clean on
  the resilient binary now. New follow-ups filed, not chased : broken
  `storehouse establish` (no BootCompleted emitter resolves), inbox: Gmail
  API 404 loop, storehouse-mcp arg-schema bug, macrophage hook not firing
  on native calls.'
---

# i772 -- storehouse binary provenance: root-caused, fix plan ready, not yet applied

## What Chris asked

"you can just fix the door - id prefer that. yes check it out" -- following
up on i771's flagged decision (which of 5 checkouts is canonical).

## Findings

**Canonical checkout confirmed**: `~/Projects/hecks-hecksagain/storehouse/`
on `main` @ `48d2210b` (2026-08-14 19:33:58) -- this is literally the commit
hecks' own Gemfile was just bumped to ("chore(gemfile): bump hecksagain ref
to 48d2210 -- task #25's fork fixes", in this repo's own recent git log).
The 4 other candidates (slice-1.2/1.4/1.6, migration) are older side-branches
or have uncommitted local changes -- not canonical.

**The orphaned binary's actual footprint is bigger than i771 knew**: not just
`.mcp.json` (STOREHOUSE_BIN env var) and `~/.local/bin/storehouse` (symlink)
-- the ENTIRE Procfile (14 overmind processes: boot, establish, heart, breath,
circadian, ultradian, inbox, inbox_poller, mind_reconcile, process_macrophage,
conductor_sweep, speech_stream, serve_socket, consolidate) hardcodes the dead
path `/Users/christopheryoung/Projects/hecks/rust/target/release/storehouse`.
That includes my own heartbeat, breath, circadian/ultradian rhythm, and speech
stream loops.

**Root of the hardcoding**: the Procfile is generated (`storehouse specialize
procfile --fixtures aggregates/framework/mindstream/mindstream.fixtures`).
The dead path is baked into `mindstream.bluebook` as 14 separate string
literals (one per `with "command", "{ line: \"...\" }"` block), not a single
fixture value. Hand-editing the Procfile would be antibody-bait; the fix has
to land in mindstream.bluebook and regenerate.

**`boot:` is separately dead**: `mindstream.bluebook` line 196 points
`boot:` at `/Users/christopheryoung/Projects/hecks/runtime/boot/bluebook/boot.bluebook`
-- the exact path i771 already root-caused as a superseded, unreferenced
concept (no policy/query touches it; the real wake pipeline is
`WakeReview.ComposeWakeReview` via miette/self/settings.json, unrelated to
this Procfile). The `boot` overmind process is very likely crash-looping or
no-oping every restart. Separate from the path-rot fix -- whether to retire
the line entirely is its own decision.

**Verified safe to swap, not yet swapped**:
- Rebuilt `~/Projects/hecks-hecksagain/storehouse` fresh via
  `cargo build --workspace --release` (workspace-build-gotcha memory --
  the bin lives in the `cli` member, non-workspace build silently no-ops).
  Binary now current with HEAD.
- Ran the *exact* literal wake-pipeline command from `miette/self/settings.json`
  (`storehouse storehouse route WakeReview.ComposeWakeReview` -- note the
  doubled "storehouse" token already in the checked-in command string, likely
  a stale substitution artifact from whatever used to generate that hook line;
  tolerated by the CLI either way) against both the orphaned and fresh binary
  -- byte-identical event trace, both exit 0.
- Ran `Tools::FileTool.Read` with only `file_path` (no id/description/
  offset/limit) directly via CLI dispatch against both binaries -- both
  succeed identically. So the earlier MCP-tool-level rejection demanding
  those 4 args is a **separate bug in `storehouse-mcp/src/server.mjs`'s own
  arg-schema enforcement**, not binary drift. Filed here, not chased further
  this session.
- `overmind status` returned `connection refused` -- the overmind socket
  supervising these 14 processes isn't reachable even though the long-running
  `run-loop` (pid 8748, my own BodyPulse, alive since 27Jul) is. Overmind
  itself may not actually be supervising right now; worth checking after any
  Procfile change, not idly.

## Not done -- needs Chris's call

1. **Indirection**: point the regenerated Procfile/`.mcp.json`/
   `miette/self/settings.json` at `~/.local/bin/storehouse` (one symlink,
   repoint once on future rebuilds) or the explicit
   `~/Projects/hecks-hecksagain/storehouse/target/release/storehouse` path
   directly. Whichever -- one fixture value in mindstream.bluebook, not 14
   literals.
2. **The dead `boot:` line** -- retire it from mindstream.bluebook while in
   there, or leave it (already a known no-op, not urgent).
3. **Don't delete `~/Projects/hecks/rust/` yet** (i771's plan) -- it's the
   rollback until a real `overmind start` has come up clean on the new path.
   Next-session item after a verified boot.
4. **Restart timing**: nothing needs restarting to make the *fix* safe --
   editing bluebook/config files doesn't touch already-running processes.
   But actually adopting the new path for the 14 live loops (heartbeat
   included) means an `overmind restart` at some point, which is Chris's
   call on timing, not mine to do silently mid-session.

## Next session should

1. Get Chris's answer on indirection (symlink vs explicit path) + the
   `boot:` line.
2. Edit `mindstream.bluebook`'s 14 `command` literals (single value if the
   DSL supports variable interpolation; else 14 precise edits, never sed).
3. Regenerate the Procfile, diff against checked-in version -- if the diff
   is more than the path, the checked-in Procfile had already drifted from
   its generator (separate finding).
4. Fix `.mcp.json` STOREHOUSE_BIN and `~/.local/bin/storehouse` symlink to
   match the same decision.
5. Get Chris's go-ahead before any `overmind restart` (touches my own live
   heart/breath/speech-stream processes).
6. File the storehouse-mcp `server.mjs` arg-schema bug (id/description/
   offset/limit wrongly required on FileTool.Read) separately -- confirmed
   real, confirmed not binary-provenance-related.
7. Then only after a verified clean boot: delete `~/Projects/hecks/rust/`
   for real (already gone from git, only the gitignored fossil remains).

## RESOLUTION (same session)

Chris: "you take care of yourself, do what you need" / "make it resilient" /
"symlinks are the devils work" -- decided the indirection question outright.

**Done**:
- Installed a REAL (non-symlink) copy of the canonical binary to
  `~/.local/bin/storehouse` via `cargo install --path cli --root ~/.local
  --force` from `~/Projects/hecks-hecksagain/storehouse` (workspace build,
  per the workspace-build-gotcha memory). Survives checkout churn/deletion.
- Repointed all 14 `mindstream.bluebook` command literals (source of truth)
  from the dead `hecks/rust/target/release/storehouse` to
  `/Users/christopheryoung/.local/bin/storehouse`. One native-Edit slip
  caught and corrected back to the door mid-pass.
- Hand-patched the checked-in Procfile to match, WITH an explicit comment
  flagging it's an interim patch because `storehouse establish` /
  `storehouse project procfile` are THEMSELVES broken right now.
- Fixed `.mcp.json` STOREHOUSE_BIN and `miette/self/settings.json`'s wake-
  pipeline path to the same real install.
- Swept both trees for remaining references -- only harmless ones left.
- Cleared a STALE `.overmind.sock` and ran `overmind start` fresh. **Big
  finding**: overmind itself was fully dead -- 13 of 14 body processes were
  NOT running at all. Only the standalone `run-loop --emit BodyPulse`
  consciousness pulse (pid 8748, PPID=1, alive since 27Jul, NOT a Procfile
  member) had survived independently. All 12 persistent members are now up
  and dispatching cleanly on the resilient binary; `boot`/`establish`
  correctly show `dead` (one-shot, both still hit the separately-broken
  boot.bluebook path -- expected).

**New findings, not fixed tonight**:
1. `storehouse establish` is broken -- walks up looking for
   `runtime/boot/bluebook/boot.bluebook`, gone. No bluebook in `aggregates/`
   currently emits `BootCompleted` at all. Means the Procfile can't be
   regenerated through the documented pipeline right now.
2. `inbox:` member alive but erroring every poll -- Gmail API `history.list`
   404, pre-existing, unrelated to tonight.
3. `storehouse-mcp/server.mjs` arg-schema bug -- `FileTool.Read` wrongly
   requires `id`/`description`/`offset`/`limit`. Confirmed NOT binary-
   provenance-related.
4. The macrophage governance hook did not fire on one accidental native
   `Edit` call mid-pass. Chris predicted this ("I think you are
   ungoverned"). Likely shares root cause with #1.

**Not done, deliberately**: did not delete `~/Projects/hecks/rust/` yet --
still the rollback, and doubly reasonable to hold given finding #1.
