---
ref: i778
status: open
priority: medium
posted_at: 2026-08-16
category: session-restart
value: 'Handoff after i777 closed the identity pass (category 1 of i776''s two-category split) -- 63/63 aggregates on branch fix/hecksagain-pin-bump-dsl-migration now carry identified_by, full corpus sweep is 53/64 valid, and every one of the 11 remaining failures is category 2 (retired impure-edge adapter primitives: macrophage, fibroblast, agent_inbox, event_sourcing, hexagon, inbox, process_health, session, tools, plan, storehouse -- 14 .hecksagon files total per i776''s count). Category 2 is real infrastructure re-expression, not mechanical, and still needs scoping with Chris before anyone touches it -- not started. Also unresolved and not chased: a storehouse__dispatch/storehouse__catalog MCP error (ArgumentError: unknown keyword: :sprint) surfaced mid-session, distinct from the identity errors, possibly related to the in-progress merge_queue.bluebook merge visible in git status (MM) -- only storehouse__validate worked around it, real dispatch was not attempted. Nothing committed -- pre-commit hook still gates on invalid=0 across all 64 roots, still 11 short.'
---

# i778 — handoff: category 1 (identity) closed, category 2 (adapter primitives) next, dispatch door still flaky

*Continuation of i776 -> i777 (2026-08-16, same day, later in the session).*

## State at handoff

- **Category 1 (identity) is CLOSED.** All 63 aggregates that were missing
  `identified_by` now have it -- see i777 for the full per-aggregate
  breakdown (29 fixed directly, 34 in `catalog/bluebook/appeal.bluebook`
  via a background agent, both reviewed and spot-checked).
- **Full sweep: 53/64 roots valid, 11 invalid.** Every remaining failure is
  category 2 -- confirmed by reading each error message, none are
  identity-shaped (`Seal refused`, `no ValueObject`, `declares no type`)
  anymore. Roots: `macrophage`, `fibroblast`, `agent_inbox`,
  `event_sourcing`, `hexagon`, `inbox`, `process_health`, `session`,
  `tools`, `plan`, `storehouse`.
- **Category 2 is untouched, not started.** Per i776: real infrastructure
  re-expression (retired `adapter :exec/:shell/:fs` kwargs forms in 14
  `.hecksagon` files), needs scoping with Chris before anyone touches it --
  each `:exec` adapter needs its real triggering event looked up, and the
  `storehouse/*.hecksagon` forms are admittedly aspirational
  (`"Until i493 lands..."`) with no real runtime behind them yet, so part
  of the scoping question is what to do with those specifically (migrate
  to a parseable stub? comment out? leave for i493?).
- **A dispatch-door bug surfaced, not chased.** Mid-session, a background
  agent working on `appeal.bluebook` hit `storehouse__dispatch` /
  `storehouse__catalog` failing with `ArgumentError: unknown keyword:
  :sprint` -- different from the CommandBus identity error (already fixed
  by the time the agent ran). `storehouse__validate` kept working
  throughout, so this didn't block the identity work, but real dispatch
  through the MCP door was never actually exercised this session as a
  result. Possibly tangled with the in-progress `merge_queue.bluebook`
  merge (`MM` in git status, unresolved).
- **Nothing committed.** Same branch, `fix/hecksagain-pin-bump-dsl-
  migration`, checked out from `main`. Pre-commit hook still gates on
  `invalid: 0` -- 11 roots short of that.

## Next session

1. Scope category 2 with Chris -- per-file, since each `:exec` adapter's
   real triggering event differs and the `storehouse/*` aspirational forms
   need a decision (migrate / stub / leave).
2. Once `storehouse__validate` reports `invalid: 0` across all 64 roots,
   the pre-commit hook unblocks the first real commit on this branch --
   squash or multiple commits is Chris's call.
3. Investigate the `:sprint` keyword error on `storehouse__dispatch`
   before relying on the door for anything beyond `validate` again.
