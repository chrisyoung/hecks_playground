---
ref: i782
status: open
priority: medium
posted_at: 2026-08-17
category: session-restart
value: 'UPDATE : picked up the smoke_fanout mechanical leftover from i781 and fixed it (with: {} wrap applied, verified via unchanged validate error that it does NOT move 60/64 -- the tools root''s invalidity is owned entirely by the separate :web_tool retired-form binding in tools.hecksagon). Chased a false alarm on identified_by (stale local vendor/hecksagain/ copy vs the real bundler-pinned gem gave two different ArgumentErrors -- advisor caught it before it went in a card ; identified_by :symbol is fine, no validator bug). Nothing committed. The three bigger design threads (for_each, run, missing handler scripts) still unpicked.'
---

# i782 — session-restart : smoke_fanout fixed (verified independent), no design thread picked yet

*Continuation of i776 -> i777 -> i778 -> i779 -> i780 -> i781, same day
(2026-08-17), later session. Filed once as orientation-only, then refreshed
after actually doing the small mechanical fix. Filing per Stop-hook feedback
so the next boot has a real handoff instead of nothing.*

## What happened this session (chronological)

- Read `inbox/i781.md` in full (the i780 `:exec` -> `spawned_by` arc,
  `bin/adapter-host` fixed and proven end-to-end, three newly-scoped design
  gaps left open).
- Corrected a Voice miscalibration (opened a boot-adjacent reply in heavy
  French, against the boot-register rule) — Chris caught it lightly, no
  further action needed.
- Chris said "continue with 782" without naming a specific thread. Picked
  the card's own recommendation : do the small mechanical leftover
  (`sprint14_smoke_fanout.hecksagon`) first, independent of the three
  bigger design calls.
- **Discovered the storehouse door cannot dispatch ANYTHING against
  `aggregates/framework/tools` right now** — confirmed live, both
  `Tools::FileTool.Read` and `Tools::FileTool.Edit` fail with
  `ArgumentError: wrong number of arguments (given 2, expected 1)`. Root
  cause (see below) is the SAME already-documented `:web_tool` retired-form
  binding i781 scoped as its own separate item — not a new bug, just newly
  hit by hand. Fell back to the native Edit tool for this one file,
  transparently, with the reason stated inline.
- **Fixed `sprint14_smoke_fanout.hecksagon`**: wrapped the bare-kwarg
  `Tools::TaskTool.Get` dispatch in `with: {}`, matching the shape the other
  4 already-fixed sites use (`dispatch(command, with: {})` on
  `DrivingAdapterBuilder::DrivenCapture`, confirmed by reading the real
  bundler-pinned gem source directly).
- **Verified the fix is correct but doesn't move `storehouse__validate`'s
  60/64** — re-ran `hecksagain-cli validate` scoped to the `tools` root
  before and after ; the error message (`given 2, expected 1`) is
  byte-identical. That root's invalidity is owned by the `:web_tool`
  binding in `tools.hecksagon` (the retired `adapter :web_tool, command:,
  tool:, result_into:` form calling the modern `adapter(name)` positional
  method with a stray kwargs hash), exactly matching i781's own diagnosis.
  Don't claim a gate improvement from this fix — there isn't one, and none
  was expected.
- **Chased a false alarm, caught it before writing it down.** While
  debugging the door failure, ran a hand-rolled Ruby script that
  `$LOAD_PATH.unshift`'d `vendor/hecksagain/lib` directly and got a
  DIFFERENT ArgumentError (`given 1, expected 0` in `identified_by`) on
  `demo`/`drafting`/`identifier_service` — roots `storehouse__validate`
  reports as clean. Looked for a moment like "the validator lies about
  validity". Called `advisor` before writing that into a card — right call.
  `identified_by`'s REAL implementation (resolved via Bundler against the
  git-pinned `hecks-hecksagain` gem, confirmed with
  `instance_method(:identified_by).source_location` /
  `.parameters`) lives in `identity_declaration.rb` and takes
  `(*targets, as:, &path)` — positional `identified_by :symbol` works fine.
  The hand script had loaded a STALE local copy under `vendor/hecksagain/`
  that doesn't match what `hecksagain-cli` actually resolves. **No
  validator bug. Nothing wrong with `identified_by`. This paragraph exists
  so nobody re-chases it** — if you see this exact `given 1, expected 0` /
  `identified_by` shape again, check your `$LOAD_PATH` before assuming a
  real defect.
- Nothing committed. `git status` on `hecks_conception` now shows one real
  content change (`sprint14_smoke_fanout.hecksagon`) beyond whatever was
  already dirty from i781's session.

## Open threads carried forward from i781 (three still open, one closed)

1. **`for_each:` inside `driven` blocks** (8 sites : `reset_story_tasks`,
   `resolve_deps` x2, `seed_sprint_active` x2, `worker_died_reclaim` x2,
   `liveness_sweep`, `expiry_sweep`, `claim_next_on_claim_released`,
   `claim_next_on_worker_registered`) — design call needed. `for_each` is
   real but only on `ProcessManagerBuilder#dispatch` (bluebook-level
   Policy) ; `DrivingAdapterBuilder::DrivenCapture#dispatch` (hecksagon-level
   driven) has no `for_each:` param. Options : extend `DrivenCapture`, or
   re-express these as Policies, or something else.
2. **Bare `run "shell cmd"` inside `driven` blocks** (5 sites :
   `pr_delivery`'s 3rd binding, `pr_mergeable_check`, `main_uptodate_check`,
   `worktree_create`, `worktree_remove`) — a second, different unbuilt
   capability. Likely wants the same `spawned_by`/effect-port shape `:exec`
   just got (i780/i781) — check that hypothesis before inventing a third
   mechanism.
3. **22 of 27 handler scripts don't exist on disk** — the adapter-host
   mechanism is proven correct but almost nothing it dispatches to is
   written yet (git/cargo/filesystem/artifact_claim/session-recall/
   sidequest_run/process_health/inbox_poll/fibroblast_sweep scripts). Most
   likely already exist as inline logic pre-migration and just need
   extracting — worth checking before writing any from scratch.
4. ~~**Small mechanical leftover** : `sprint14_smoke_fanout.hecksagon`~~ —
   **done this session.** `with: {}` wrap applied, verified independent of
   the `tools` root's `invalid` status (that root's failure belongs to the
   `:web_tool` binding, see below — unaffected by this fix).

Also still open, unchanged from i781, still needing Chris :
- `tools` (`:web_tool` adapter — no `Adapters::Web`-style implementation
  exists anywhere in the fork ; this is now the ONLY thing keeping the
  `tools` root `invalid`, since #4 above is done).
- `agent_inbox`/`event_sourcing` (driving-side scheduler — cron AND
  interval both confirmed unbuilt, not just interval).
- `storehouse` (`:fs`/`:shell` — still i778's "until i493" call).

## Next session

1. **Pick one of threads #1/#2/#3 above** — no answer from Chris yet on
   which. #4 is done, don't re-offer it.
2. Nothing new committed on `fix/hecksagain-pin-bump-dsl-migration` this
   session (one working-tree edit, `sprint14_smoke_fanout.hecksagon`,
   uncommitted) — still 4 roots short of `invalid: 0` at the validate
   level ; #4's fix does not change that count, verified, don't re-check it.
3. **Not a bug, but a reflex worth keeping**: if `Hecks.boot`/anything under
   `vendor/hecksagain/lib` throws an error shape that doesn't match what
   `hecksagain-cli` reports for the same root, check `$LOAD_PATH` /
   Bundler resolution before trusting the hand-rolled repro — `vendor/
   hecksagain/` can hold a stale copy that diverges from the real
   git-pinned gem. Cost a false-alarm chase this session ; advisor caught
   it before it became a written finding.
