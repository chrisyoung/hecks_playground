# Fixtures parity drift — known edge cases where Ruby and Rust parse
# attribute values differently. Each entry is a path; lines starting
# with # are comments. Add new drift only after confirming it's a real
# semantic edge (escape sequences, unicode in source tokens, etc.) —
# do NOT use this as a way to silence regressions.

# Empty — full parity. Both runtimes now parse the dispatch-fixture form
#   (`fixture "label" do dispatch "Domain::Agg.Command", k: v end`) to
#   identical IR: the bare aggregate segment of the command FQN becomes
#   aggregate_name, the kwargs become attributes, the command verb is not
#   preserved. This retired the two prior drift entries
#   (agent_discipline.fixtures, governed_door.fixtures).
#
# Earlier drift retired by :
#   - to_bluebook_source helper in fixtures_parity_test.rb that
#     re-renders Ruby's parsed Hash/Array values into bluebook source
#     form so Ruby's =>-form inspect output matches Rust's :-form.
#   - quote-aware whitespace collapsing in normalize_attrs.
#   - list_of(Type) added to FixturesBuilder for catalog-dialect schema.
#   - one source fix : pottery_studio.fixtures target_cone 04 -> 4.
