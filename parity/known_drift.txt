# Parity Suite — known drift list
#
# Fixtures listed here are EXPECTED to disagree between the Ruby DSL parser
# and the Rust hecks-life parser. The pre-commit hook reports them as ⚠
# (warning, not error). Adding to this list is an antibody failure — do it
# only when the divergence is documented and accepted as a real semantic gap.
#
# When you fix a divergence, REMOVE its line. If a known-drift fixture
# accidentally starts passing, the suite will tell you to celebrate and
# remove it from this list.
#
# Format: <relative-path>  # reason

# (empty — full parity 113/113 as of 2026-04-18)

hecks_conception/nursery/verbs/verbs.bluebook  # Ruby DSL rejects `aggregate "Lexicon", "description" do` with arity error — pre-existing regression unrelated to Phase F-2. Inbox: nursery-verbs-parity-regression.

hecks_conception/capabilities/antibody/fixtures/antibody.fixtures  # Ruby fixtures parser cannot read the catalog-dialect `schema:` kwarg form introduced in PR #401 (i42). Ruby returns nil ; Rust parses cleanly. Accepted drift — Rust is the authoritative parser, Ruby is winding down. This entry retires with the whole parity/ machinery when Ruby parity is formally cut from CI.

hecks_conception/aggregates/disposition.bluebook         # then_set :field, from: :param (i106 dsl-mutation-primitives) not yet in Ruby DSL — Rust parses, Ruby raises "unknown keyword: :from". Retires when Ruby DSL absorbs the i106 from: form.
hecks_conception/aggregates/psychic_link.bluebook        # then_set :field, from: :param (i106 dsl-mutation-primitives) not yet in Ruby DSL — same drift as disposition.
hecks_conception/aggregates/section_template.bluebook    # then_set :field, from: :param (i106 dsl-mutation-primitives) not yet in Ruby DSL — same drift as disposition.
hecks_conception/aggregates/wake_ritual.bluebook         # then_set :field, from: :param (i106 dsl-mutation-primitives) not yet in Ruby DSL — same drift as disposition.
