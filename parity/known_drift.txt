# Parity Suite — known drift list
#
# Fixtures listed here are EXPECTED to disagree between the Ruby DSL parser
# and the Rust storehouse parser. The pre-commit hook reports them as ⚠
# (warning, not error). Adding to this list is an antibody failure — do it
# only when the divergence is documented and accepted as a real semantic gap.
#
# When you fix a divergence, REMOVE its line. If a known-drift fixture
# accidentally starts passing, the suite will tell you to celebrate and
# remove it from this list.
#
# Format: <relative-path>  # reason

# (empty — full parity 113/113 as of 2026-04-18, then drift below as new gaps surface)

hecks_conception/aggregates/demo/demo.bluebook  # `belongs_to Story from Plan` — the English ` from <Context>` cross-context qualifier (Relationship grammar, replacing the codey `Context::X`) is space-separated and NOT Ruby-eval-compatible : the Rust text-parser reads it, the legacy Ruby DSL evaluates `from` as a bare method call on AggregateBuilder and errors. Same class as the north-star FQN-verb binds (a66c9dccd) ; retires with the Ruby parser lift. (2026-06-18)

# inbox.bluebook drift retired 2026-06-12 — fixed in the parser projection :
# parse_aggregate (parser_shape) now collects policies declared INSIDE an
# aggregate body and bubbles them to domain.policies via the dispatch call
# site, mirroring Ruby's AggregateBuilder + canonical collect_all_policies
# flatten. inbox's LockOnSignoff now serialises on both sides (i602).

# nursery/verbs drift retired 2026-04-30 — the failure was inside the
# Lexicon aggregate (given :verb, not_in: :known_verbs structured form
# + IndexOnRegister policy without trigger). Permissive Ruby `given`
# (kwargs ignored) and `policy.build` (trigger optional) match Rust's
# silent-empty parses ; verbs now parses byte-equal.

# antibody.fixtures entry retired 2026-04-30 — vestigial. parity_test.rb
# only scans .bluebook files ; the catalog-dialect schema: parity is
# covered by parity/fixtures_parity_test.rb (its own known_drift.txt),
# not by this list. Removing the orphaned entry.

# i106 `from:` drift retired 2026-04-30 — added `from:` kwarg to Ruby's
# then_set + a matching `from:` arm in Rust parse_blocks. Both parsers
# now carry the symbol form (`:param`) so the canonical IR matches
# byte-for-byte. Four fixtures (disposition, psychic_link,
# section_template, wake_ritual) move from drift to parity.

# spec.bluebook drift retired — root cause was a Rust parse_command bug,
# not a JSON serialization quirk. parse_command consumed lines past a
# bare `command "Reset"` (no do-block), eating the closing `end` of the
# enclosing aggregate and walking the rest of the file as if it were
# inside one mega-command. Fixed in rust/src/parse_blocks.rs by
# returning early when the first line of a command lacks a do-block.
# All 12 spec aggregates now parse byte-equal between Ruby and Rust.

# Escape-handling drift retired 2026-05-09 — fixed in
# rust/src/parser_helpers.rs : `extract_string` now respects
# backslash escape sequences inside double-quoted DSL strings,
# unescaping them the way Ruby's parse semantics do (\\ → \,
# \" → ", \n → newline, \t → tab, etc.). Iterates by char (not
# byte) to keep UTF-8 multi-byte sequences intact. Both
# cli_dispatch_shape.bluebook and parser_helpers_shape.bluebook
# parse byte-equal again — neither carries truncated vision
# strings any more.

# synapse.bluebook drift retired 2026-05-09 — fixed in
# rust/src/parser_helpers.rs : `ends_with_do_block` now returns
# false for any line whose trimmed form starts with `#`, so
# commented `do` lines no longer increment the parser's depth
# counter. Ruby's parser already ignored them natively ; both sides
# now agree. synapse.bluebook parses byte-equal again.

# dispatch_audit.bluebook drift retired 2026-05-09 — fixed in two
# places : (a) ruby/hecks/dsl/command_builder.rb's `role` method
# now accepts the i483 typed form (Role + `as:` / `kind:` kwargs,
# accepted-and-ignored until the IR-lift follow-up lands) ; (b)
# rust/src/parse_blocks.rs's role parsing now handles the bareword
# form (`role Role`) without falling back to extract_string —
# critically, this means a trailing `kind: "system"` argument no
# longer leaks into the role name. Both parsers extract "Role"
# byte-equal. dispatch_audit parses byte-equal again.

# harry_wingate.bluebook drift retired 2026-06-12 — fixed in the parser
# projections : codegen/parser_helpers_shape gains a comment-aware
# strip_trailing_comment (a `#` inside a quoted default is preserved), and
# parse_blocks_shape's parse_attribute strips the trailing Ruby comment
# before extracting the positional type. Ruby's lexer drops comments
# natively ; both parsers now agree. harry_wingate parses byte-equal again.

# voice_latency.bluebook + storehouse_log.bluebook drift retired 2026-06-13 —
# fixed in the Ruby reference (parity-harness wrap) : QueryRecorder
# (bluebook_model/behavior/query.rb) gains an `attribute` recorder method and
# Query#record_block_structure! merges the recorded attributes, so a
# query-block `attribute :x, T` declaration now lands in the canonical query
# IR on both sides. Both parse byte-equal again.

# conductor.bluebook drift retired 2026-06-13 — the `delivery :actor` keyword
# was vestigial : captured by neither IR (the Rust parser silently ignored it)
# and selecting a runtime delivery-mode fork that was itself already retired
# (command_dispatch.rs : "legacy delivery-mode fork retired"). Removed the dead
# keyword from conductor.bluebook rather than teach Ruby to swallow it. Both
# parsers now read conductor identically. (If actor-delivery lands later it
# returns properly : grammar + both IRs + actual consumption.)
