[package]
name = "storehouse-cli"
version = "0.1.0"
edition = "2021"
description = "storehouse CLI — the composition root. Wires concrete adapters (sqlite, ...) into the runtime and exposes the storehouse command surface. The binary is named `storehouse`."

[[bin]]
name = "storehouse"
path = "src/main.rs"

[dependencies]
storehouse = { path = ".." }
storehouse-sqlite = { path = "../sqlite" }
serde_json = "1"
