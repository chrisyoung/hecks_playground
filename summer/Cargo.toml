[package]
name = "summer"
version = "0.2.0"
edition = "2021"
description = "Spring — the universal domain command line"

[dependencies]
hecks-life = { path = "../rust" }
serde_json = "1"
libc = "0.2"
